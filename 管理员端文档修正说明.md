# 项目文档修正说明

对照项目代码逐一核查 `项目文档/管理员端功能说明/` 下 6 份文档，修正了以下错误和不准确之处。

---

## 一、管理员端课程监管功能-技术深度解析.md

| 修改位置 | 原内容 | 修正后 | 修正原因 |
|---|---|---|---|
| 接口总数 | 72个管理员端API接口 | 75个 | CourseAdminController 8个（非7）、ChapterAdminController 8个（非7）、SectionAdminController 10个（非9），逐一对照代码更正 |
| CourseAdminController 接口数 | 7 | 8 | 代码中实际有8个方法 |
| ChapterAdminController 接口数 | 7 | 8 | 代码中实际有8个方法 |
| SectionAdminController 接口数 | 9 | 10 | 代码中实际有10个方法 |
| CourseCommentAdminController 方法名 | getCommentsPage | getCommentList | 代码中实际方法名为 getCommentList |
| CourseAdminController 方法名 | getStatistics | getCourseStatistics | 代码中实际方法名为 getCourseStatistics |
| 前端 course/api.js 函数列表 | 仅列10个 | 补充5个章节相关函数 | 代码实际有15个函数，缺少 getChapters/addChapter/updateChapter/deleteChapter/batchChapter |
| 前端 course-comment/api.js 函数列表 | 仅列9个 | 补充 getCourses | 代码实际有10个函数，缺少 getCourses |
| 前端页面列表 | 缺少作业相关页面 | 补充 homework/homework-submission 行 | 文档遗漏了作业管理页面 |
| 已知问题 | 无 #21/#22 | 新增 #21 小节状态切换 PATCH/PUT 不匹配、#22 my-course 批量退课路径不匹配 | 前端 api.js 用 PUT 但后端用 PATCH + RequestBody；前端调 /batch-quit 但后端为 /batch |

---

## 二、管理员端订单功能-技术深度解析.md

| 修改位置 | 原内容 | 修正后 | 修正原因 |
|---|---|---|---|
| 中等问题 #6 | 无 | 新增：分页参数名不匹配 | 前端发送 page/size，后端 OrderQueryDTO 期望 pageNum/pageSize，分页功能失效 |
| 中等问题 #7 | 无 | 新增：前端未定义函数调用 | getStatistics 的 catch 块调用 calculateStatisticsFromList()，该函数未定义 |
| 中等问题 #8 | 无 | 新增：api.js JSDoc 参数名错误 | 注释写 params.courseId，实际参数名为 goodsId |

---

## 三、管理员端数据大屏功能-技术深度解析.md

| 修改位置 | 原内容 | 修正后 | 修正原因 |
|---|---|---|---|
| 统计仪表板端点数 | "11个分散的 /api/admin/* 端点" | "10个分散的 /api/admin/* 端点（11次API调用，orders/statistics 被调两次）" | 实际只有10个不同端点，但 getOrderStats 和 getRevenueStats 调用同一端点 |
| 功能清单 | 缺少"收入统计"行 | 补充收入统计行，注明与订单统计同端点、重复调用 | 代码中 getRevenueStats 调用同一 /api/admin/orders/statistics |
| 功能清单末尾 | 无补充说明 | 补充 api.js 共13个函数、2个未使用、同端点重复请求的说明 | 代码中有 getCourseStudyStats/getUserStudyStats 未在统计页面使用 |
| Promise.allSettled 调用列表 | 10个调用 | 11个调用（补充 getRevenueStats） | 代码中实际有11个Promise |
| 收入计算描述 | "从 recentOrders 数组筛选DONE状态累加 goodsPrice" | "totalRevenue 直接取自API返回值；completedOrdersRevenue 和 averageOrderValue 仍为客户端二次计算；同端点重复请求" | 代码中 revenueStats.totalRevenue 取自 API 响应的 totalRevenue 字段，非客户端计算 |
| 已知问题 #6 | "统计仪表板收入客户端计算" | "统计仪表板收入部分客户端计算"，更新说明 | totalRevenue 已由后端返回，但 completedOrdersRevenue/averageOrderValue 仍前端计算 |

---

## 四、管理员端用户管理功能-技术深度解析.md

| 修改位置 | 原内容 | 修正后 | 修正原因 |
|---|---|---|---|
| 问题 #10 严重度及描述 | 低 / "调用参数冗余" | 高 / "角色管理 update 调用导致 URL 错误" | `api.update(formData.id, formData)` 传数字作为首个参数，data.id 为 undefined，请求 URL 变为 /api/admin/role/undefined，角色更新功能不可用 |
| 问题 #12 | "预留接口未使用" / 低 | "预留接口无后端实现" / 中 | 前端定义的 batchDelete/getPermissions/assignPermissions 在后端 RoleController 中无对应端点，调用返回 404 |
| 问题 #15 | "编辑用户不传递密码" | "编辑用户 payload 包含后端忽略的字段" | handleSubmit 展开含 username/password 的 payload，但 UpdateUserDTO 无这些字段，被 Spring 静默忽略 |
| 新增问题 #16 | 无 | "用户表单缺少 avatar 字段" | CreateUserDTO/UpdateUserDTO 支持 avatar，但前端表单无头像输入 |
| RoleController 端点表 | 缺少"角色详情"行、路径写法有误 | 补充 GET /{id} 行，修正 PUT //{id} 为 PUT /{id}，增加端点总数说明 | 代码中有 getRoleById 方法；原文路径多了一个 / |
| role/api.js 表 | batchDelete/getPermissions/assignPermissions 标注"⚠ 未调用" | 改为"⚠ 无后端实现，调用返回 404" | 这三个函数的后端端点不存在，不仅仅是未调用 |

---

## 五、管理员端数据大屏-设计方案.md

| 修改位置 | 原内容 | 修正后 | 修正原因 |
|---|---|---|---|
| 后端接口设计 | 仅列9个粗粒度端点 | 标注为"设计方案"，补充实际实现与设计的差异说明 | 设计方案与最终实现差异较大（9个→14个端点），需标注以免混淆 |
| 功能优先级 | 仅标注 ✅/⚠️ | 补充各条目的具体实现端点名，新增"设计方案中列出但实际额外实现的功能"列表 | 标明哪些设计项已实现、哪些未实现，以及实现中额外增加的功能 |

---

## 六、管理员端运营配置功能-技术深度解析.md

核查后确认文档内容与代码一致，无需修改。已有已知问题（发布/撤回 PUT vs POST 不匹配、缺少批量删除端点等）均已正确记录。

---

## 修正统计

| 文档 | 修正条目数 |
|---|---|
| 课程监管功能-技术深度解析 | 10 |
| 订单功能-技术深度解析 | 3 |
| 数据大屏功能-技术深度解析 | 6 |
| 用户管理功能-技术深度解析 | 7 |
| 数据大屏-设计方案 | 2 |
| 运营配置功能-技术深度解析 | 0 |
| **合计** | **28** |
