# 公共参考文档修正汇总

> 修正时间：2026-05-11
> 对照项目代码逐一核对 `项目文档/公共参考/` 目录下 14 个文档，修正与实际代码不符之处。

---

## 一、数据库表结构汇总.md（修正最多，约 50 处）

基于 `mqxq/sql/mqxq结构.sql` 逐一比对每张表定义，修正内容：

| 表名 | 修正内容 |
|------|---------|
| user | 移除不存在的 `email` 字段；`phone` VARCHAR(20)→VARCHAR(11)；`avatar` VARCHAR(500)→VARCHAR(255)；`name` VARCHAR(100)→VARCHAR(255) NOT NULL；`password` NOT NULL→NULL；添加缺失的 `create_user`/`update_user` |
| role | `role_name` VARCHAR(50) NOT NULL→VARCHAR(255) NULL；移除不存在的 `description`；添加缺失的 `update_time` |
| user_role | 移除不存在的 FK 约束；添加缺失的 `create_time`/`update_time`；移除不存在的索引 |
| course | `title` VARCHAR(200)→VARCHAR(50)；`status` INT→TINYINT；`duration_sum` 描述"分钟"→"秒"；添加缺失的 `create_user`/`update_user`/`update_time` |
| course_subject | `name` VARCHAR(100)→VARCHAR(50)；移除不存在的 `sort_order`；添加缺失的审计字段 |
| chapter | `title` VARCHAR(200)→VARCHAR(50)；移除不存在的 `sort_order`；添加缺失的审计字段 |
| section | 移除不存在的 `sort_order`；添加缺失的 `course_id`/`content`/`status`/审计字段；`title` VARCHAR(200)→VARCHAR(50)；`video_url` VARCHAR(500)→VARCHAR(255) |
| orders | 移除不存在的 `total_amount`；`order_no` VARCHAR(50) UNIQUE→VARCHAR(255) NULL；`pay_time`/`create_time` VARCHAR(50)→VARCHAR(255) |
| my_course | 移除不存在的 `current_section_id`/`progress`/`join_time`/`last_study_time`；`status` INT→TINYINT；修正索引 |
| course_view | 添加缺失的 `ip_address`/`user_agent`/`create_time`；修正索引 |
| course_homework | `max_score`(INT,DEFAULT 100)→`score`(DECIMAL(5,2),DEFAULT 100.00)；添加缺失的 `answer`/`update_user`/`update_time` |
| homework_submission | 添加缺失的 `max_score`/`grade_user`/`create_user`/`update_user`/`create_time`/`update_time`；添加 UNIQUE 索引 |
| chat_session | `session_id` UNIQUE→NOT NULL(仅有索引)；添加缺失的 `create_user`/`updater_user`；修正索引名 |
| chat_message | 添加缺失的 `idx_create_time` 索引 |
| announcements | `publisher_id`→`create_user`；移除不存在的 `status`；`content` TEXT→LONGTEXT；添加缺失的 `update_user`/`update_time` |
| 新增 | 完整添加遗漏的 `announcement_categories` 表 |
| carousel | `image_url`→`carousel_url`；`sort_order`→`sort`；`status`→`is_deleted`；添加缺失的审计字段 |
| course_comment | 添加缺失的 `teacher_id`/`update_time`；`content` TEXT→VARCHAR(1000)；`score` INT→TINYINT |

---

## 二、API使用说明.md（7 处修正）

| # | 修正内容 |
|---|---------|
| 1 | API 结构树缺少 `chat.ts` 和 `types/chat.ts`，已补充 |
| 2 | 导入示例缺少 `chatApi`，已补充 |
| 3 | 分页数据字段名 `records`→`list`（后端返回 `list` 非 `records`） |
| 4 | `userApi.purchaseCourse()` 不存在，改为 `userApi.createCourseOrder()` |
| 5 | `cancelOrder()` 参数类型 `orderId(number)`→`orderNo(string)` |
| 6 | RegisterRequest 字段修正：移除不存在的 `email`/`nickname`，`emailCode`→`code`，`userType` 数字→字符串 |
| 7 | 错误提示说明修正：API 拦截器使用 `console.error` 而非 `ElMessage` |

---

## 三、系统级已知问题与风险清单.md（5 处修正）

| # | 修正内容 |
|---|---------|
| 1 | 删除"认证系统指南.md 中误写 is-concurrent: true, is-share: false"的错误断言，该指南实际未直接列出这些值 |
| 2 | 删除"后端聊天实现说明.md 中误写 topK=999"的错误断言，该文档已正确记录 topK=10, similarityThreshold=0.7 |
| 3 | carousel 表 `is_deleted` 描述修正：字段在查询中过滤 `is_deleted=0`，但删除操作仍使用物理删除 `deleteById`，`is_deleted` 实际始终为 0 |
| 4 | 退款端点权限描述修正：AliPayController 类上有 `@SaCheckRole("用户")`，但 SaInterceptor 排除了 `/api/alipay/**` 导致注解不生效，`POST /api/payment/refund` 端点在代码中不存在 |
| 5 | 支付接口安全性描述修正：精确说明 SaInterceptor 的 excludePathPatterns 使类级注解失效，仅 `/notify` 回调确实需要排除 |

---

## 四、向量库同步机制说明.md（1 处修正）

| # | 修正内容 |
|---|---------|
| 1 | 增量同步 Cron 表达式 `0 0 */1 * * ?`→`0 0 * * * ?`，与 `CourseEmbeddingScheduledTask.java` 第 88 行代码文本一致 |

---

## 五、认证系统指南.md（2 处修正）

| # | 修正内容 |
|---|---------|
| 1 | `@SaCheckRole` 示例从英文 role_code（"ADMIN"/"TEACHER"/"USER"）改为中文 role_name（"管理员"/"教师"/"用户"），与 `StpInterfaceImpl.getRoleList()` 实际查询 `role_name` 字段一致 |
| 2 | 登录响应示例添加缺失的 `phone` 字段，`roles` 从 `["USER"]` 修正为 `["用户"]` |

---

## 六、系统基础设施配置汇总.md（4 处修正）

| # | 修正内容 |
|---|---------|
| 1 | Elasticsearch URL `http://localhost:19200`→`http://159.75.11.181:19200`，与 application.yml 激活值一致 |
| 2 | CourseEmbeddingScheduledTask 增量 Cron `0 0 */1 * * ?`→`0 0 * * * ?`，与代码文本一致 |
| 3 | MyLoggerAdvisor 使用端从"学生端+教师端"→"教师端（YuManus）"，该 Advisor 仅在 YuManus.java 中注册 |
| 4 | ReReadingAdvisor 使用端从"学生端+教师端"→"⚠ 存在但未注册到任何端"，该类存在但未在任何配置中引用；QuestionAnswerAdvisor 补充说明"chat方法内联添加" |

---

## 七、系统业务流程与模拟数据说明.md（14 处 API 路径修正）

| # | 错误路径 | 正确路径 |
|---|---------|---------|
| 1 | `POST /api/teacher/course/create` | `POST /api/teacher/course` |
| 2 | `POST /api/teacher/chapter/create` | `POST /api/teacher/chapter` |
| 3 | `POST /api/teacher/section/create` | `POST /api/teacher/section` |
| 4 | `POST /api/teacher/homework/create` | `POST /api/teacher/homework` |
| 5 | `GET /api/public/course/list` | `GET /api/public/course/page` |
| 6 | `POST /api/user/order/create` | `POST /api/user/orders/create-course-order/{courseId}` |
| 7 | `POST /api/user/alipay/pay` | `GET /api/alipay/pay` |
| 8 | `POST /api/user/alipay/callback` | `POST /api/alipay/notify` |
| 9 | `POST /api/user/comment/create` | `POST /api/user/comment` |
| 10 | `GET /api/public/comment/list` | `GET /api/public/comment/course/{courseId}` |
| 11 | `GET /api/teacher/comment/list` | `GET /api/teacher/comment/course/{courseId}` |
| 12 | `POST /api/admin/comment/update-status` | `PUT /api/admin/comment/{id}/toggle-status` |
| 13 | 作业相关多个路径错误 | 修正为实际 Controller 路径 |
| 14 | 学习状态相关多个路径错误 | 修正为实际 Controller 路径 |

---

## 八、Excel导出功能说明.md（4 处修正）

| # | 修正内容 |
|---|---------|
| 1 | 导出接口数量 6→7，补充遗漏的 `GET /api/teacher/homework-manage/export-submissions/{homeworkId}` |
| 2 | 教师端接口数 4→5 |
| 3 | Swagger 密码从硬编码 `123456` 改为引用 application.yml 配置 |
| 4 | API 文档 URL 以 `/doc.html`（Knife4j）为主要地址，`/swagger-ui/index.html` 为备选 |

---

## 九、MinIO功能说明.md（11 处修正）

| # | 修正内容 |
|---|---------|
| 1 | 标题和简介从"代码修复总结/修复完成时间"改为"代码问题与建议修复/文档更新时间"，明确修复尚未应用到代码 |
| 2 | delete() 方法描述修正：`bucket` 变量并非"定义了没使用"，而是"使用了URL解析的bucket而非配置中的bucket" |
| 3 | FileController 返回类型列表从 4 个补全为 7 个（6 个返回 String + 1 个返回 Result） |
| 4 | saveFileAuto() 的 .pptx 支持：标注为当前代码未包含（仅有 .ppt），属于建议修复 |
| 5 | ContentType 枚举：`DOC`→`WORD`（实际枚举名为 WORD）；补全完整枚举值列表（10→25 个） |
| 6 | 调用统计表接口数 6→7 |
| 7 | 头像上传场景代码：标注当前代码使用硬编码 `ContentType.PNG`，动态选择为建议修复 |
| 8 | 异常处理描述：从"所有MinIO操作异常包装为MyException"修正为"uploadFile和uploadCompressImg包装为MyException；delete和downLoadFile仅 e.printStackTrace()" |
| 9 | 日志描述：标注 delete() 和 downLoadFile() 缺少成功日志 |
| 10 | 优缺点分析：移除不准确的"参数验证完善""异常处理统一""资源管理规范""日志记录完整"等描述，新增"异常处理不统一""资源泄漏""返回类型不统一"等不足 |
| 11 | 总结部分：代码质量从"优秀"改为"待改进（存在11个待修复问题）"；推荐配置中 readPath 标注为"已配置但代码未使用，可考虑移除" |

---

## 十、无需修正的文档

| 文档名 | 说明 |
|--------|------|
| 公共数据模型与基础配置.md | 逐项对照代码验证，均正确 |
| 浏览记录功能完整说明.md | 所有 API 路径和数据类型与代码一致 |
| 学习状态自动流转实现说明.md | 所有接口路径和类名与代码一致 |
| Vue表格分页和滚动配置指南.md | UI 配置指南，与代码一致 |
| 数据大屏-公共设计说明.md | 设计建议文档，与代码一致 |

---

## 修正统计

| 文档 | 修正数量 |
|------|---------|
| 数据库表结构汇总.md | ~50 |
| 系统业务流程与模拟数据说明.md | 14 |
| MinIO功能说明.md | 11 |
| API使用说明.md | 7 |
| 系统级已知问题与风险清单.md | 5 |
| 系统基础设施配置汇总.md | 4 |
| Excel导出功能说明.md | 4 |
| 认证系统指南.md | 2 |
| 向量库同步机制说明.md | 1 |
| **合计** | **~98** |
