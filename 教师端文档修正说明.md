# 教师端文档修正说明

对照项目代码逐文件检查教师端功能说明文档，修正与实际代码不符的描述。以下按文档分列所有修改项。

---

## 一、教师端课程功能-技术深度解析.md

| # | 修改位置 | 原内容 | 修正后 | 依据 |
|---|---|---|---|---|
| 1 | onMounted 生命周期 | `Promise.all` 并行请求 | 改为独立调用（各自 await） | `course/index.vue` 实际为顺序独立调用 |
| 2 | formData.lessonNum 默认值 | 默认 1 | 默认 0 | `ref({ lessonNum: 0 })` |
| 3 | CreateCourseDTO 字段 | 包含 lessonNum/durationSum | 删除此二字段并加警告 | 后端 CreateCourseDTO 无此字段，前端发送但后端静默忽略 |
| 4 | SectionStatusUpdateDTO 交互 | 未说明前后端不一致 | 加警告：前端以查询参数传 status，后端期望 @RequestBody | `sectionManage/api.js` 传 params，后端 @RequestBody |
| 5 | 章节标题最大长度 | max 50 | max 200（后端）/ max 50（前端） | CreateSectionDTO @Size(max=200)，前端 n-input maxlength=50 |
| 6 | 章节内容最大长度 | max 500 | max 5000（后端）/ max 500（前端） | CreateSectionDTO @Size(max=5000)，前端 n-input maxlength=500 |
| 7 | 新增问题 #13-#17 | — | CreateCourseDTO 缺字段、section status 前后端不匹配、校验差异、死代码 API、未使用 getSimpleCourses | 代码对照结果 |

---

## 二、教师端课程学生管理功能-技术深度解析.md

| # | 修改位置 | 原内容 | 修正后 | 依据 |
|---|---|---|---|---|
| 1 | 端点表格 | 仅列部分端点 | 补充 CourseHomeworkManageController 8 个端点 + HomeworkSubmissionTeacherController 8 个端点 | 控制器实际存在 |
| 2 | replyComment 描述 | "API 已定义" | "仅在前端 api.js 定义，后端无此端点，调用会 404" | 后端无对应映射 |
| 3 | MyCourseQueryDTO | 缺少字段 | 补充 userId/orderId/userNickname/courseName + 分页警告 | DTO 实际字段对照 |
| 4 | CommentQueryDTO | 缺少字段 | 补充 userId/teacherId/limit/days | DTO 实际字段对照 |
| 5 | 新增问题 #14-#17 | — | 缺少控制器、3处分页参数不匹配(page/size vs pageNum/pageSize)、死代码、幽灵端点 | 代码对照结果 |

---

## 三、教师端作业功能-技术深度解析.md

| # | 修改位置 | 原内容 | 修正后 | 依据 |
|---|---|---|---|---|
| 1 | homework/api.js 函数名 | getHomeworkList/publishHomework/getHomeworksByCourse | getList/publish/getCourseHomework | api.js 实际导出名 |
| 2 | homework/api.js | 包含 getSubmissions 等函数 | 移除，注明属于 grade/submission-manage | 各 api.js 文件实际归属 |
| 3 | submission-manage/api.js | getList/getDetail/getStatistics | getSubmissionList/getSubmissionDetail/getSubmissionStatistics | api.js 实际导出名 |
| 4 | submission-manage/api.js | gradeSubmission 参数 | 修正为 `(submissionId, data) => request.put(..., { submissionId, ...data })` | api.js 实际签名 |
| 5 | grade/api.js | 仅 2 个函数 | 扩展为 6 个（getSubmissions/getHomeworkSubmissions/getCourses/getHomeworks） | api.js 实际导出 |
| 6 | checkTeacherHomeworkPermission | 参数顺序 (id, teacherId) | (teacherId, homeworkId) | Java 方法实际签名 |
| 7 | statistics 页面 | Promise.all | 独立调用 | index.vue 实际代码 |
| 8 | 新增问题 #17-#21 | — | 批量批改请求体严重不兼容(frontend:{submissionIds,score,comment} vs backend:{gradings:[{studentId,score,teacherComment}]})、分页参数 size/pageSize、title 参数被忽略、BASE_URL 未定义、gradeForm.score 初始 0 | 代码对照结果 |

---

## 四、教师端数据大屏功能-技术深度解析.md

| # | 修改位置 | 原内容 | 修正后 | 依据 |
|---|---|---|---|---|
| 1 | 布局图 | 含[自动刷新]按钮 | 移除，实际代码无此按钮 | index.vue 无自动刷新功能 |
| 2 | statistics 页面 | Promise.all | 独立调用 | statistics/index.vue 实际代码 |
| 3 | course-view 页面 | Promise.all | 移除，注明 getMyCourses 未被调用 | course-view/index.vue 实际代码 |
| 4 | 浏览器检测 | 排除 Edge | 实际代码不排除 Edge | 检测逻辑不含 Edge 排除 |
| 5 | 旧 index.vue 描述 | 未说明数据未渲染 | "JS 中加载了统计数据但模板未渲染，页面显示默认欢迎页" | 旧 index.vue 模板无统计组件 |
| 6 | 订单数据映射 | 缺 payNo | 补充 payNo 字段 | OrderVO 含 payNo |
| 7 | 订单详情 | 仅 queryById | 注明 controller 传 teacherId | controller 实际传 teacherId |
| 8 | 新增问题 #14-#16 | — | 旧 index.vue 数据未渲染、Edge 浏览器误判、course-view/api.js 孤立函数 | 代码对照结果 |

---

## 五、教师端和管理员端登录功能-技术深度解析.md

| # | 修改位置 | 原内容 | 修正后 | 依据 |
|---|---|---|---|---|
| 1 | userinfo 端点 URL | `/api/auth/user-info` | `/api/auth/userinfo`（无连字符） | CompleteUnifiedAuthController @GetMapping("/userinfo") |
| 2 | check-email 端点 | 无警告 | 加警告：后端无此端点，调用会 404 | 后端无 check-email 映射 |
| 3 | changePassword/updateProfile | PUT 方法 | 加警告：前端 PUT，后端 @PostMapping，会 405 | 控制器实际用 @PostMapping |
| 4 | LoginRequest 校验 | `@NotBlank, @Size 1-50` | 仅 `@NotBlank`，无 @Size 约束 | LoginRequest.java 实际注解 |
| 5 | Redis 验证码 key | `"CAPTCHA:{captchaKey}"` | `"login_captcha:{captchaKey}"` | LoginConstants.LOGIN_CAPTCHA = "login_captcha:" |
| 6 | Token 刷新方法 | `StpUtil.refreshToken()` | `StpUtil.renewTimeout(getTokenTimeout())` | UnifiedAuthServiceEnhanced 实际调用 |
| 7 | isRemember | key='loginInfo-isRemember', 默认 false | key='isRemember', 默认 true | `useStorage('isRemember', true)` |
| 8 | loginInfo | `reactive({username:'', password:''})` | `ref({username:'teacher1', password:'123456'})` | index.vue 实际声明 |
| 9 | 验证码 MIME 类型 | `image/png` | `image/jpeg` | ImageIO.write(image, "jpg", ...) |
| 10 | 验证码类型 | 未说明为算术表达式 | 注明 Kaptcha 算术表达式（如 `3+5=?`） | CaptchaTextCreator 生成算术题 |
| 11 | API 表 getUserInfo | 无警告 | 加警告：前端发 /user-info，后端映射 /userinfo，会 404 | 前后端路径不一致 |
| 12 | 架构图 user-info | `/api/auth/user-info` | `/api/auth/userinfo` | 控制器实际映射 |
| 13 | 新增问题 #17-#22 | — | check-email 幽灵端点、HTTP 方法不匹配(405)、LoginRequest 无长度约束、验证码类型描述错误、loginInfo 默认值不符、Token 刷新机制描述错误 | 代码对照结果 |

---

## 六、教师端AI教案功能-技术深度解析.md

| # | 修改位置 | 原内容 | 修正后 | 依据 |
|---|---|---|---|---|
| 1 | 控制器方法名 | `chatWithManus` | `doChatWithManus` | ManusTeacherController 实际方法名 |
| 2 | compressToolResponse | 保留开头/结尾各 500 字符 | headLength=1000, tailLength=900（动态计算） | BaseAgent: `headLength = maxToolResponseLength/2`, `tailLength = maxToolResponseLength - headLength - 100` |
| 3 | 任务完成关键词 | "已经完成"/"task is complete" | 实际无此二词；新增"完成"/"任务结束"/"已结束"/"结束"/"task completed"/"all done"等14个 | ToolCallAgent isTaskCompleted 实际数组 |
| 4 | 继续关键词 | "还需要继续"/"but"/"however"/"yet" | 实际无此四词；新增"下一步"/"继续"/"待处理"；"还需要"重复6次疑为 bug | ToolCallAgent continuationKeywords 实际数组 |
| 5 | TerminalOperationTool | `Runtime.getRuntime().exec()` | `ProcessBuilder`（Runtime.exec 已注释） | TerminalOperationTool.java 实际代码 |
| 6 | TerminalOperationTool 超时 | "60秒强制终止" | 无超时，`process.waitFor()` 无超时参数，无限期阻塞 | TerminalOperationTool.java 无 timeout 参数 |
| 7 | WordGenerationTool | 未提及 sanitizeFileName | 注明内含 sanitizeFileName()（移除特殊字符、限长度200、确保.docx后缀） | WordGenerationTool.java 第606-640行 |
| 8 | 问题 #3 路径遍历 | 无 sanitizeFileName 说明 | 补充说明：生成端有 sanitizeFileName 保护，但下载端点本身未做路径遍历校验 | 代码对照结果 |
| 9 | 新增问题 #19-#22 | — | 控制器方法名错误、TerminalOperationTool 无超时保护(升级为高危)、continuationKeywords 重复项、FileCleanupTool 未注册 | 代码对照结果 |

---

## 七、教师端数据大屏-设计方案.md

| # | 修改位置 | 原内容 | 修正后 | 依据 |
|---|---|---|---|---|
| 1 | 后端接口设计 | 8 个设计方案端点 | 对照表：2 个一致、4 个合并入 overview、1 个拆分为3个独立端点、1 个未实现 | DashboardTeacherController 实际端点 |
| 2 | 未列出实际额外端点 | 无 | 补充 6 个实际额外端点（course-status-distribution 等） | 控制器实际映射 |
| 3 | 功能优先级标记 | 仅 ✅/⚠️ 无说明 | 每项注明是否实现及对应组件 | dashboard/index.vue 实际功能 |
| 4 | 未实现功能清单 | 无 | 列出 4 个未实现图表（收入来源分布饼图、作业提交情况柱状图、作业平均分趋势折线图、作业完成率饼图） | 前后端无对应代码 |
| 5 | 未规划已实现功能 | 无 | 列出 2 个设计中未规划但已实现功能（课程状态分布饼图、课程完成率排行） | 代码对照结果 |
| 6 | recent-orders | ⚠️ 标记 | 升级为 ❌ 未实现 | 后端无端点、前端无表格 |

---

## 修改统计

| 文档 | 修改项数 | 新增问题数 |
|---|---|---|
| 课程功能 | 6 | 5 |
| 课程学生管理 | 4 | 4 |
| 作业功能 | 7 | 5 |
| 数据大屏功能 | 7 | 3 |
| 登录功能 | 12 | 6 |
| AI教案功能 | 8 | 4 |
| 数据大屏设计方案 | 6 | — |
| **合计** | **50** | **27** |
