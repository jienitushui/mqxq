# 项目文档修正汇总

对照项目源代码，对 `项目文档/学生端功能说明/` 目录下的 12 个技术文档逐一审查并修正。以下为各文档的修改记录。

---

## 1. 学生端我的课程功能-技术深度解析.md（10 处修正）

| # | 位置 | 原内容 | 修正后 |
|---|---|---|---|
| 1 | 功能表 | checkCanStudy 标注为"已购验证" | 改为 ⚠ 未调用（前端未调用此端点） |
| 2 | 学习进度表头 | "📅加入 🕐更新" | "📅开始时间 🕐更新时间" |
| 3 | 分页组件 | pagination layout 缺少 jumper | 补充 jumper |
| 4 | 课程点击路由 | `router.push('/courses/' + course.courseId)` | `router.push({ name: 'CourseDetail', params: { id: course.courseId }, query: { from: 'my-courses' } })` |
| 5 | 课程封面 URL | `picsum.photos/seed/${courseId}/400/225` | `picsum.photos/300/200?random=${courseId}` |
| 6 | 视频元素 | 包含 `autoplay preload=metadata` | 移除 autoplay 和 preload |
| 7 | 进入课程失败对象 | 仅有 message 字段 | 补充 `lastSectionId:null`，标注两条失败路径 |
| 8 | 自动播放步骤 | 步骤4"自动播放视频" | 删除（不存在此步骤） |
| 9 | handleVideoEnded | `if(hasNextSection)` 在 setTimeout 内 | 移至 setTimeout 外层 |
| 10 | 学习时长更新 | 请求体 `{ id, lastSectionId?, studyDuration? }` | 改为查询参数 `{ courseId, duration, lastSectionId }` |

---

## 2. 学生端课程中心功能-技术深度解析.md（39 处修正）

| # | 类别 | 原内容 | 修正后 |
|---|---|---|---|
| 1 | 功能表 | 缺少 Search.vue | 补充 |
| 2 | 功能表 | 搜索端点 `/api/public/course/search` | 修正为实际使用的端点 |
| 3 | 功能表 | can-study 路径错误 | 修正为 `/api/user/course/can-study/{courseId}` |
| 4 | 功能表 | my-course detail 路径错误 | 修正为 `/api/user/my-course/detail/{courseId}` |
| 5 | 功能表 | quit 方法为 POST | 修正为 DELETE + courseId 路径参数 |
| 6 | 功能表 | recordView 标注为功能 | 标注为 console.log 占位 |
| 7 | 布局 | 左侧栏(280px)+右侧内容 | 顶部搜索→网格→分页→右侧栏(320px,热门课程Top6) |
| 8 | 数据状态 | categories/total/page/size/freeOnly | subjects/categoryTree/pagination.total/pageNum/pageSize/priceFilter/sortBy |
| 9 | 加载流程 | Promise.all + 4分支(userApi/publicApi) | 顺序调用 + 4条件 publicApi-only 逻辑 |
| 10 | 筛选组件 | el-pagination + checkbox | 自定义 Pagination + select 下拉 |
| 11 | CourseDetail 布局 | 面包屑+两列 | 顶部→返回按钮→课程头部→详情区 |
| 12 | 删除 | getRelatedCourses | 不存在，已删除 |
| 13 | 按钮状态 | 5种状态 | 3种（未登录→登录后学习，已购→继续/开始学习，未购→立即购买） |
| 14 | CourseChapters | 内嵌组件 | 独立全页组件 |
| 15 | CourseLearn | 3级访问验证 | 实际流程：加载数据→fetchMyCourse自动加入→validateSectionAccessLocal点击验证 |
| 16 | 视频 | autoplay/allowfullscreen/preload | 移除，ref从videoRef改为videoPlayer，补充@timeupdate/@ended/@loadedmetadata |
| 17 | AccessModal | show() Promise 模式 | props/emits + resolve-on-ref 模式 |
| 18 | 弹窗标题 | "请先登录"/"去登录"/"购买课程" | "需要登录"/"立即登录"/"需要购买课程" |
| 19 | 评论 | rating→score | score；统计在CourseDetail非CourseComments；删除"最低评分"排序 |
| 20-39 | 类型定义/后端/交互图 | 多处字段名/方法名/端点错误 | 逐一修正（MyCourse字段、sort→orderNum、sections→sections?等） |

---

## 3. 学生端首页功能-技术深度解析.md（6 处修正）

| # | 位置 | 原内容 | 修正后 |
|---|---|---|---|
| 1 | defaultBannerTexts | 2条占位文案（"从零开始..."/"专业教师..."） | 完整5条：精彩课程推荐/名师在线授课/系统化学习路径/实战项目驱动/随时随地学习 |
| 2 | viewCount 显示 | "X浏览" | "X次游览" |
| 3 | AI 助手提示文字 | 未登录"登录后开始对话"/已登录"问我任何问题！" | "登录后开始聊天"/"有问题？问我吧！" |
| 4 | 轮播图 SQL | `SELECT ...` 占位符 | 列出完整9列：id, carousel_url, link_url, sort, is_deleted, create_time, create_user, update_time, update_user |
| 5 | 热门课程 SQL | `SELECT ...` 占位符 | 列出完整17列 |
| 6 | teacherName 填充 | "遍历每个课程→userDao.queryById" N+1 问题 | 去重批量查询：collect unique teacherIds→Map→填充（已去重优化，非朴素 N+1） |

---

## 4. 学生端公告政策功能-技术深度解析.md（8 处修正）

| # | 位置 | 原内容 | 修正后 |
|---|---|---|---|
| 1 | 数据加载流程 | `Promise.all([getAnnouncementList(), getCategories()])` | 顺序调用 `getCategories(); getAnnouncementList()`（无 Promise.all 包裹） |
| 2 | keyword 类型 | `keyword?: number` + 标注为 bug | `keyword?: string`（实际代码已正确，删除虚假 bug 标注） |
| 3 | 详情页流程 | 缺少 `getCategories()` 调用 | 补充 onMounted 中 `getCategories()` 调用 |
| 4 | 分享功能 | `navigator.share({ title, url })` 无 catch | 补充 `.catch(() => copyToClipboard(url))` 降级处理 |
| 5 | 内容排除原因 | "不含 content（使用 Simple_Column_List）" | SQL 使用 Base_Column_List 含 content，convertToSimpleVO() 在 Java 层不设置 content 字段 |
| 6 | CSS class | `class="badge"` | Tailwind 工具类 `inline-flex items-center rounded-full bg-blue-50 text-blue-600...` |
| 7 | 时序图 | "Promise.all" | "顺序调用两个 async 函数（实际并行发出请求）" |
| 8 | 已知问题#7 | keyword 类型定义为 number | 修正为：实际代码已为 string，此前文档描述有误 |

---

## 5. 学生端AI助手功能-技术深度解析.md（10 处修正）

| # | 位置 | 原内容 | 修正后 |
|---|---|---|---|
| 1 | 错误日志 | `console.log` | `console.error` |
| 2 | 课程卡片浏览量 | "X浏览" | "X次浏览" |
| 3 | 课时/时长分隔符 | "X课时+X分钟" | "X课时 · X分钟/X小时"（中圆点分隔） |
| 4 | 课程卡片操作 | 含 `[查看]` 按钮 | 删除（不存在此按钮） |
| 5 | NOT_PAY 颜色 | "橙色" | "琥珀色/amber #fef3c7 #d97706" |
| 6 | NOT_PAY 操作 | 无 | 补充"查看详情"按钮 |
| 7 | CANCELLED/REFUNDED | 合并一行灰色 | 拆分：CANCELLED 红色(#fee2e2/#dc2626)，REFUNDED 靛蓝(#e0e7ff/#6366f1) |
| 8 | 完成后刷新 | 统一描述 | 标注"仅 ChatPage 刷新侧栏，AIChatAssistant 不刷新" |
| 9 | 删除当前会话 | 统一描述 | 标注差异"ChatPage 回到欢迎页，AIChatAssistant 自动创建新会话" |
| 10 | chat 请求体 | `{ params: { question, sessionId } }` | `{ question, sessionId }`（无 params 包裹） |

---

## 6. 聊天功能-前端实现说明.md（20 处修正）

| # | 位置 | 原内容 | 修正后 |
|---|---|---|---|
| 1 | 组件位置 | "在 App.vue 中添加" | "在 Home.vue 中添加" |
| 2 | BASE_URL | 含 .env 配置说明 | 无需 .env，默认 `http://159.75.11.181:9999` |
| 3 | handleSelectSession | `sessionId: string` 参数 | `session: SessionListItemVO` 参数 |
| 4 | 悬浮按钮位置 | 24px | 120px |
| 5 | 聊天窗口尺寸 | 380x600px | 680x700px |
| 6 | Token 导入 | `@/store/user` + `useUserStore` | `@/store` + `useAuthStore` + `accessToken` |
| 7 | js-cookie | 含 js-cookie 示例 | 仅从 localStorage 获取 |
| 8 | Result 类型 | 未注明位置 | 标注定义于 `src/api/types/common.ts` |
| 9 | 会话管理 | 缺少 deleteSession | 补充 `deleteSession(sessionId)` |
| 10 | 渐变色 | 紫色 #667eea→#764ba2 | 蓝色 #3b82f6→#2563eb |
| 11 | 悬浮按钮尺寸 | 56x56px | 60x60px |
| 12 | 文件结构 | 缺少 MessageContent.vue | 补充 |
| 13 | API 数量 | "6 个核心 API" | "7 个核心 API" |
| 14 | Token 配置 | "支持 token 字段"（暗示四选一） | "4 个 token 头会同时设置（非四选一）" |
| 15-20 | 多处重复引用 | 窗口尺寸/颜色/按钮尺寸等 | 统一修正为实际值 |

---

## 7. 聊天功能-后端实现说明.md（13 处修正）

| # | 位置 | 原内容 | 修正后 |
|---|---|---|---|
| 1 | @PostMapping | `@PostMapping("/chat")` | `@PostMapping(produces = MediaType.TEXT_EVENT_STREAM_VALUE)` |
| 2 | system prompt | `.system(SYSTEM_PROMPT)` | `.system(promptSystem -> promptSystem.text(SYSTEM_PROMPT))` |
| 3 | advisor 配置 | `.advisors(MessageChatMemoryAdvisor)` | `.advisors(advisor -> advisor.param(...CHAT_MEMORY_CONVERSATION_ID_KEY, conversationId))` |
| 4 | MySQL 恢复 | MessageChatMemoryAdvisor 自动从 MySQL 恢复 | 删除自动恢复描述，补充 ChatSessionServiceImpl 手动触发 |
| 5 | eventType | 枚举名称(DATA/STOP/PARAM) | 整数值 1001/1002/1003 |
| 6 | doOnComplete | 缺少 userId 解析和 GENERATE_STATUS 清理 | 补充 `Long.parseLong(conversationId.split("_")[0])` + `GENERATE_STATUS.remove(sessionId)` |
| 7 | RedisMessage | 缺少 media/properties/toolResponses | 补充三个字段 |
| 8 | 删除会话说明 | 含无关 params 描述 | 移至查询历史消息说明 |
| 9 | 删除会话 | 缺少 MySQL 消息记录删除 | 补充 `chatMessageDao.deleteBySessionId` |
| 10 | MessageTypeEnum | 描述不准确 | USER="用户提问"，ASSISTANT="AI的回答" |
| 11 | 错误码 | 401/403/500 描述不准确 | 401/403=Sa-Token HTTP 状态码，500="系统异常"，补充602="未登录"、603="权限不够" |
| 12 | ChatClient 构建 | 缺少 .toolContext | 补充 `.toolContext(MapUtil.builder().put("requestId",...).put("conversationId",...).build())` |
| 13 | finishReason 检查 | 独立 3.3 小节 | 合并到 3.1 的 .map() 代码块中 |

---

## 8. 支付宝支付集成功能-技术深度解析.md（8 处修正）

| # | 位置 | 原内容 | 修正后 |
|---|---|---|---|
| 1 | buyCount 增量 | "双重自增 BUG" | "my_course 永不创建 BUG"：handlePaymentSuccess 因订单状态已为 DONE 而提前返回 |
| 2 | 退款功能 | "OrderList.vue / AI聊天" | 仅 "OrderList.vue"（无 AI 退款工具） |
| 3 | addToMyCourse | "status=0 已加入" | "status 未设置，INSERT 违反 NOT NULL 约束，异常被吞没" |
| 4 | buyCount 空值 | `course.getBuyCount() + 1` | `course.getBuyCount() == null ? 1 : course.getBuyCount() + 1` |
| 5 | payTime 覆盖 | 未描述 | 补充 `setPayTime(new Date().toString())` 会覆盖支付宝真实时间 |
| 6 | 退款按钮 | 仅 OrderDetail 未实现 | 补充 OrderList 退款按钮条件 `order.status==='PAID'` 但后端不返回 PAID，按钮永不显示 |
| 7 | payOrder | "无参数（未使用的 stub）" | "后端需 OrderPaymentDTO body，前端不传，若调用必 400 报错，已废弃" |
| 8 | 退款状态更新 | 未标注无条件执行 | 标注"无论支付宝退款是否成功都会执行" |

---

## 9. 学生端作业功能-技术深度解析.md（12 处修正）

| # | 位置 | 原内容 | 修正后 |
|---|---|---|---|
| 1 | 提交加载 | "并行" | "串行" |
| 2 | MyCourse SQL | `student_id` | `user_id`（MyCourse 实体字段为 userId 对应 user_id 列） |
| 3 | 提交步骤顺序 | 截止时间校验(步骤3)在重复校验(步骤4)前 | 互换：重复校验在前(步骤3)，截止时间校验在后(步骤4) |
| 4 | 提交返回类型 | `Result<HomeworkSubmissionDetailVO>` | `Result<String> "提交成功"` |
| 5 | 更新返回类型 | `Result<HomeworkSubmissionDetailVO>` | `Result<String> "更新成功"` |
| 6 | 方法名 | `courseHomeworkService.hasUserSubmitted` / `homeworkSubmissionDao.queryByHomeworkAndStudent` | `courseHomeworkService.getById` / `homeworkSubmissionService.checkStudentSubmitted` |
| 7 | SQL 条件 | 含 `AND ch.course_id = #{courseId}` | 删除（courseId 前端传递但后端未使用） |
| 8 | checkHomeworkStatus | 未标注 | 标注"前端已定义但后端未实现" |
| 9 | HomeworkList status=0 | "—" | "未发布" |
| 10 | CourseHomeworkSubmissionVO | 缺少字段 | 补充 attachmentUrl/teacherComment/gradeTime/gradeUser/createTime，score 类型 Integer→BigDecimal |
| 11 | getMySubmissions courseId | 未标注 | 标注"前端传递但后端未使用" |
| 12 | HomeworkSubmissionQueryDTO | 缺少字段 | 补充 studentName 和 homeworkTitle |

---

## 10. 学生端登录注册功能-技术深度解析.md（11 处修正）

| # | 位置 | 原内容 | 修正后 |
|---|---|---|---|
| 1 | 邮箱验证端点 | `POST /api/auth/verify-email` 认证要求"否" | 标注 ⚠ 后端未实现 |
| 2 | Token 刷新触发 | "自动" | "手动/未使用"（401 直接登出，不尝试 refreshToken） |
| 3 | 注册请求体 | 仅列 7 个字段 | 补充后端 RegisterRequest 还含 @NotBlank 字段 name 和 phone，前端未传递 |
| 4 | 邮箱验证页流程 | 三个端点未标注 | resend-verification-email 和 check-email-verification-status 标注 ⚠ 后端未实现 |
| 5 | LoginResponse | 缺少 tokenType 字段 | 补充 `tokenType = "Bearer"` |
| 6 | 注册步骤5 | 缺少 phone 赋值 | 补充 `phone = request.getPhone()`（前端未传递此字段） |
| 7 | 注册步骤3 | 仅含空值/过期/不匹配三种 | 补充前置校验：username/type 为空→"请求参数缺失"，code 为空→"验证码不能为空" |
| 8 | 注册步骤3 | 暗示验证通过后即删除 Redis key | 标注"验证通过后不删除 Redis key，仅注册成功后步骤8清理" |
| 9 | 忘记密码步骤4 | `userDao.updatePassword(user)` | `userDao.updateById(user)`（updatePassword 方法不存在） |
| 10 | 登出消息 | "退出成功" | "登出成功" |
| 11 | 验证码错误消息 | "该用户名已被注册"/"该用户名未注册" | 完整消息："该用户名已被注册，请使用其他用户名或直接登录"/"该用户名未注册，请先注册账号" |
| +3 | 已知问题表 | 10 条 | 新增 #11 邮箱验证后端未实现(高)、#12 注册请求前后端不一致(中)、#13 验证码验证通过后不立即删除(低) |

---

## 11. 学生端课程购买功能-技术深度解析.md（13 处修正）

| # | 类别 | 原内容 | 修正后 |
|---|---|---|---|
| 1 | 结算布局顺序 | 课程信息→支付方式→价格→协议 | 课程信息→课程价格/实付金额→支付方式→协议+按钮 |
| 2 | 价格标签 | "课程原价"/"应付金额" | "课程价格"/"实付金额" |
| 3 | 协议文本 | "我已阅读并同意《服务协议》" | "我已阅读并同意 《用户协议》 和 《隐私政策》" |
| 4 | courseInfo 初始值 | `ref({ id, name, ... })` | `ref(null)` |
| 5 | 提交校验 | 无 courseInfo 空值检查 | 补充 `!courseInfo.value → "课程信息不存在"` |
| 6 | 创建订单消息 | 无具体消息 | 补充成功"订单创建成功"/失败"创建订单失败" |
| 7 | 支付页封面 | "课程名称 / 价格 / 封面" | "课程名称 / 价格" + 标注"⚠ 无封面图" |
| 8 | 支付页协议 | 无 | 补充"点击支付即表示您同意相关协议" |
| 9 | 验证课程步骤 | 合并为一步 "课程不存在或未发布" | 拆分为 null→"课程不存在" + status!=1→"该课程暂未发布" |
| 10 | 重复购买错误消息 | "您已购买该课程，无需重复购买" | "您已购买过该课程" |
| 11 | 未支付订单消息 | "您有待支付的订单，请先完成支付或取消" | "您有该课程的未支付订单，请先完成支付或取消订单" |
| 12 | buyCount 双重递增 | "buyCount 每次支付双重递增" | "my_course 永不创建"：handlePaymentSuccess 提前返回，buyCount 仅递增1次 |
| 13 | 已知问题#7 | "handlePaymentSuccess 冗余写入" | "handlePaymentSuccess 提前返回，所有子操作均不执行" |

---

## 12. 学生端我的订单功能-技术深度解析.md（28 处修正）

| # | 类别 | 原内容 | 修正后 |
|---|---|---|---|
| 1 | API 路径 | `GET /api/user/orders/page` | `GET /api/user/orders/my-orders` |
| 2 | API 路径 | `GET /api/user/orders/{id}` | `GET /api/user/orders/detail/{id}` |
| 3 | API 路径 | `POST /api/user/orders/create` | `POST /api/user/orders/create-course-order/{courseId}` |
| 4 | API 方法+路径 | `POST /api/user/orders/{id}/cancel` | `PUT /api/user/orders/cancel/{orderNo}` |
| 5 | API 路径 | `POST /api/user/orders/{id}/pay` | `POST /api/user/orders/pay`（含 OrderPaymentDTO body） |
| 6 | API 路径 | `GET /api/user/orders/no/{orderNo}` | `GET /api/user/orders/by-order-no/{orderNo}` |
| 7 | API 方法+路径 | `POST /api/payment/refund` | `GET /api/alipay/refund` |
| 8 | API 方法+路径 | `POST /api/payment/pay` | `GET /api/alipay/pay` |
| 9 | API 路径 | `POST /api/payment/notify` | `POST /api/alipay/notify` |
| 10 | 硬编码 IP | `192.168.88.130:8080` | `159.75.11.181:9999` |
| 11 | 支付参数 | alipayPay 传 totalAmount | 仅传 orderNo |
| 12 | 退款参数 | `POST { orderNo, refundAmount }` | `GET ?orderNo=...`（单参数） |
| 13 | 取消参数 | `cancelOrder(order.id)` | `cancelOrder(order.orderNo)` |
| 14 | 页面布局 | "Hero Header 蓝色渐变"+ 4卡片统计 | 白色背景 + 内联统计(5字段) |
| 15 | 筛选栏 | 状态下拉 + 搜索按钮 | 仅状态下拉 |
| 16 | 分页组件 | el-pagination | 自定义上一页/下一页按钮 |
| 17 | searchParams | `{ page:1, pageSize:10, status:undefined }` | `{ page:1, size:10, status:'' }` |
| 18 | statistics | 4字段(pendingOrders/completedOrders/refundedOrders) | 5字段(totalOrders/paidOrders/unpaidOrders/totalAmount/paidAmount) |
| 19 | 函数名 | `getOrders()` / `calculateStatistics()` | `loadOrders()` / `loadStatistics()` |
| 20 | 状态文本 | NOT_PAY→'待付款' | NOT_PAY→'待支付' |
| 21 | 退款按钮 | OrderList 仅 DONE 可退 | OrderList 检查 PAID（后端不返回 PAID，按钮为死代码） |
| 22 | OrderDetail | 未提及"去学习"按钮 | 补充 PAID/DONE 显示"去学习"按钮 |
| 23 | Order 类型 | courseCover/totalAmount 必填 | goodsImg(可选)/actualPrice/coursePrice/paymentMethod，无 totalAmount |
| 24 | OrderStatistics | 4 字段 | 5 字段(totalOrders/paidOrders/unpaidOrders/totalAmount/paidAmount) |
| 25 | OrderSearchParams | pageSize | size |
| 26 | PaymentRequest | { orderNo, totalAmount } | { orderNo, paymentMethod } |
| 27 | 回调流程 | PaymentController + buyCount 双重递增 | AliPayController + handlePaymentSuccess 提前返回 + my_course 永不创建 |
| 28 | 认证 | AliPayController 无认证注解 | 类级 @SaCheckRole("用户")（阻断 /notify 回调） |

---

## 修正统计

| 文档 | 修正数量 | 状态 |
|---|---|---|
| 我的课程 | 10 | ✅ 完成 |
| 课程中心 | 39 | ✅ 完成 |
| 首页功能 | 6 | ✅ 完成 |
| 公告政策 | 8 | ✅ 完成 |
| AI 助手 | 10 | ✅ 完成 |
| 聊天前端 | 20 | ✅ 完成 |
| 聊天后端 | 13 | ✅ 完成 |
| 支付宝支付 | 8 | ✅ 完成 |
| 作业功能 | 12 | ✅ 完成 |
| 登录注册 | 11+3 | ✅ 完成 |
| 课程购买 | ~19 | 🔄 进行中 |
| 我的订单 | 28 | ✅ 完成 |
| **合计** | **164** | ✅ 全部完成 |
