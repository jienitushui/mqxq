# 学生端我的订单功能 — 技术深度解析

## 一、功能概述

学生端订单模块提供订单列表浏览、状态筛选、订单详情查看、取消/退款/支付重试等操作，涉及前端列表页、详情页、AI 聊天订单卡片，以及后端用户/管理员/教师三套 Controller 与支付回调。

### 功能清单

| 功能 | 前端页面/组件 | 后端端点 | 认证要求 |
|---|---|---|---|
| 我的订单列表 | OrderList.vue | `GET /api/user/orders/page` | `@SaCheckLogin + @SaCheckRole("用户")` |
| 订单详情 | OrderDetail.vue | `GET /api/user/orders/{id}` | 同上 |
| 创建课程订单 | Checkout.vue / AI 助手 | `POST /api/user/orders/create` | 同上 |
| 取消订单 | OrderList.vue | `POST /api/user/orders/{id}/cancel` | 同上 |
| 支付订单 | OrderList.vue | `POST /api/user/orders/{id}/pay` | 同上 |
| 按订单号查询 | 支付回调 | `GET /api/user/orders/no/{orderNo}` | 同上 |
| 退款 | OrderList.vue / OrderDetail.vue | `POST /api/payment/refund` | ⚠ 无认证注解 |
| 支付宝支付 | OrderList.vue / Checkout.vue | `POST /api/payment/pay` | ⚠ 无认证注解 |
| 支付回调 | 支付宝服务器 | `POST /api/payment/notify` | 无（支付宝回调） |
| 管理员订单列表 | 管理后台 | `GET /api/admin/orders/page` | `@SaCheckRole("管理员")` |
| 管理员订单详情 | 管理后台 | `GET /api/admin/orders/{id}` | 同上 |
| 管理员删除订单 | 管理后台 | `DELETE /api/admin/orders/{id}` | 同上 |
| 管理员统计 | 管理后台 | `GET /api/admin/orders/statistics` | 同上 |
| 教师课程订单 | 管理后台 | `GET /api/teacher/orders/my-course-orders` | `@SaCheckRole("教师")` |
| 教师收入趋势 | 管理后台 | `GET /api/teacher/orders/my-revenue` | 同上 |
| 教师热门课程 | 管理后台 | `GET /api/teacher/orders/top-courses` | 同上 |
| 教师订单详情 | 管理后台 | `GET /api/teacher/orders/{id}` | 同上 ⚠ 无归属校验 |

### 订单状态流转

> 完整的订单状态定义、流转图和前后端映射差异见 [公共参考-订单状态体系](../../公共参考/公共数据模型与基础配置.md#一订单状态体系)

**学生端多组件状态映射差异**：OrderList、OrderDetail、MessageContent(AI订单卡片) 三个组件对状态的展示和判断存在不一致，具体包括：
- OrderList 缺少 COMMENT_DONE 映射，AI 组件使用 CANCELLED（双写 L）而非后端的 CANCEL
- OrderDetail 定义了 PAID 状态但后端不使用，且缺少 CANCEL 和 COMMENT_DONE 映射
- MessageContent 额外定义了 REFUND_PENDING 和 REFUNDED 状态但后端未使用

---

## 二、前端关键逻辑

### 2.1 订单列表页（OrderList.vue）

#### 页面布局

```
┌──────────────────────────────────────────────┐
│ Navbar                                       │
├──────────────────────────────────────────────┤
│ Hero Header（蓝色渐变）                       │
│ "我的订单" 标题 + 统计信息                      │
├──────────────────────────────────────────────┤
│ 筛选栏: [状态下拉] [搜索按钮]                   │
├──────────────────────────────────────────────┤
│ 统计卡片行                                    │
│ ┌────────┬────────┬────────┬────────┐       │
│ │全部订单  │待付款   │已完成   │已退款   │       │
│ │ XX笔    │XX笔    │XX笔    │XX笔    │       │
│ └────────┴────────┴────────┴────────┘       │
├──────────────────────────────────────────────┤
│ 订单卡片列表                                  │
│ ┌────────────────────────────────────────┐   │
│ │ [封面图]  订单号: ORD...               │   │
│ │           课程名称                      │   │
│ │           ¥价格    状态标签              │   │
│ │           创建时间                      │   │
│ │           [取消] [退款] [去支付]         │   │
│ └────────────────────────────────────────┘   │
├──────────────────────────────────────────────┤
│ 分页控件                                     │
└──────────────────────────────────────────────┘
```

#### 数据状态

```typescript
const loading = ref(false)
const orders = ref<Order[]>([])
const total = ref(0)
const searchParams = reactive<OrderSearchParams>({
  page: 1, pageSize: 10, status: undefined
})

// ⚠ 客户端统计，仅基于当前页数据
const statistics = ref<OrderStatistics>({
  totalOrders: 0, pendingOrders: 0,
  completedOrders: 0, refundedOrders: 0
})
```

#### 数据加载流程

```
onMounted → getOrders()
│
├── userApi.getMyOrders(searchParams)
│   → GET /api/user/orders/page?page=1&pageSize=10&status=
│
├── ⚠ 双重响应格式解包:
│   ├── 格式A: response.data?.records (Spring Boot PageHelper 分页)
│   └── 格式B: response.data?.list (旧格式兼容)
│   → orders.value = records || list || []
│   → total.value = response.data?.total || response.data?.totalCount || 0
│
├── calculateStatistics()
│   ⚠ 仅基于当前页 orders 数组计算，非全局统计
│   ├── totalOrders = orders.length
│   ├── pendingOrders = orders.filter(o => o.status === 'NOT_PAY').length
│   ├── completedOrders = orders.filter(o => o.status === 'DONE').length
│   └── refundedOrders = orders.filter(o => o.status === 'REFUND_DONE').length
│
└── ⚠ 统计数值随翻页变化，用户看到的统计不反映真实全局数据
```

#### 筛选与分页

| 操作 | 触发方式 | 逻辑 |
|---|---|---|
| 状态筛选 | select change | `searchParams.status = val` → `searchParams.page = 1` → `getOrders()` |
| 分页翻页 | el-pagination | 更新 page → `getOrders()` |
| 每页条数 | el-pagination sizes | 更新 pageSize + page=1 → `getOrders()` |

#### 取消订单

```typescript
const cancelOrder = async (order: Order) => {
  await ElMessageBox.confirm('确定要取消该订单吗？', '提示', { type: 'warning' })
  await userApi.cancelOrder(order.id)
  ElMessage.success('订单已取消')
  getOrders()  // 刷新列表
}
```

仅 NOT_PAY 状态的订单显示"取消"按钮。

#### 退款

```typescript
const refundOrder = async (order: Order) => {
  await ElMessageBox.confirm('确定要申请退款吗？', '提示', { type: 'warning' })
  const result = await paymentApi.alipayRefund(order.orderNo, order.totalAmount)
  // ⚠ 调用真实退款 API: POST /api/payment/refund
  if (result.code === 200) {
    ElMessage.success('退款申请已提交')
  }
  getOrders()
}
```

仅 DONE 状态的订单显示"退款"按钮（OrderList 中）。

#### 支付重试

```typescript
const retryPayment = (order: Order) => {
  paymentApi.alipayPay(order.orderNo, order.totalAmount)
  // ⚠ 实际实现: window.location.href = `http://192.168.88.130:8080/api/payment/pay?...`
  // 硬编码后端 IP，非 Axios 请求
}
```

仅 NOT_PAY 状态的订单显示"去支付"按钮。

#### 状态文本映射

```typescript
const getStatusText = (status: string | number) => {
  const map: Record<string | number, string> = {
    'NOT_PAY': '待付款', 'DONE': '已完成',
    'CANCEL': '已取消', 'REFUND_DONE': '已退款'
  }
  return map[status] || '未知状态'
}
```

⚠ 缺少 PAID、CANCELLED、REFUNDED、COMMENT_DONE 映射，与后端实际可能返回的状态值不匹配。

#### 订单时间显示 Bug

```typescript
const formatTime = (time: string) => {
  if (!time) return '--'
  return new Date(time).toLocaleString()
}

// ⚠ OrderDetail 中: updateTime.value = order.value.createTime
// 将更新时间硬编码为创建时间，updateTime 信息丢失
```

### 2.2 订单详情页（OrderDetail.vue）

#### 页面布局

```
┌──────────────────────────────────────────────┐
│ 面包屑: 首页 > 我的订单 > 订单详情              │
├──────────────────────────────────────────────┤
│ 订单状态区                                    │
│ ┌────────────────────────────────────────┐   │
│ │ [状态图标]  状态标题(大字)               │   │
│ │            状态描述文字                  │   │
│ └────────────────────────────────────────┘   │
├──────────────────────────────────────────────┤
│ 订单信息卡片                                  │
│ 订单号: ORD...  [复制]                       │
│ 创建时间: ...                                │
│ 支付时间: ...                                │
│ 支付方式: 支付宝（从 payNo 推断）             │
├──────────────────────────────────────────────┤
│ 课程信息卡片                                  │
│ [封面图]  课程名称                            │
│           ¥价格                              │
│           [查看课程] → ⚠ 跳转 CourseDetail   │
├──────────────────────────────────────────────┤
│ 操作按钮区                                    │
│ [去支付] / [取消订单] / [申请退款] / [返回]    │
└──────────────────────────────────────────────┘
```

#### 三态渲染

| 状态 | 条件 | 显示 |
|---|---|---|
| 加载中 | `loading === true` | 旋转图标 |
| 正常内容 | `order !== null` | 完整订单详情 |
| 不存在 | `order === null` | 错误提示 + 返回按钮 |

#### 数据获取流程

```
onMounted → getOrderDetail()
│
├── const id = Number(route.params.id)
├── 参数校验: id 为 falsy → ElMessage.error('订单ID无效')
│
├── userApi.getOrderDetail(id)
│   → GET /api/user/orders/{id}
│   → 后端校验订单归属当前用户
│
├── 双重 fallback: response.data || response
│
└── 赋值 order + 状态/时间解析
    ├── statusTitle / statusDesc / statusIcon 状态映射
    ├── payTime 格式化
    └── ⚠ updateTime = createTime（Bug）
```

#### 状态映射（更丰富但仍有缺陷）

```typescript
const getStatusInfo = (status: string | number) => {
  const map = {
    'NOT_PAY': { title: '待付款', desc: '请尽快完成支付', icon: 'Clock' },
    'PAID':    { title: '已支付', desc: '支付成功，课程学习中', icon: 'Check' },
    'DONE':    { title: '已完成', desc: '订单已完成', icon: 'Success' },
    'CANCEL':  { title: '已取消', desc: '订单已取消', icon: 'Close' },
    'REFUND_DONE': { title: '已退款', desc: '退款已完成', icon: 'Refresh' }
  }
  // ⚠ PAID 存在但后端不使用；缺少 COMMENT_DONE
}
```

#### ⚠ 退款功能为假实现

```typescript
const refundOrder = async () => {
  await ElMessageBox.confirm('确定要申请退款吗？')
  // ⚠ 注释: "这里需要调用退款接口"
  ElMessage.success('退款申请已提交')  // 仅显示成功提示
  // ⚠ 实际未调用任何退款 API
}
```

OrderDetail 的退款是纯 UI 假动作，与 OrderList 调用真实 `paymentApi.alipayRefund` 形成矛盾。

#### 退款按钮可见性不一致

| 组件 | 退款按钮显示条件 |
|---|---|
| OrderList.vue | 仅 DONE 状态 |
| OrderDetail.vue | DONE 或 PAID 状态 |

#### 课程导航

```typescript
const goToCourse = () => {
  router.push({ name: 'CourseDetail', params: { id: order.value.courseId } })
  // ⚠ 跳转到课程详情页而非课程学习页
  // 已购买课程应跳转 CourseLearn 更合理
}
```

#### 支付方式推断

```typescript
const getPaymentMethod = (payNo: string) => {
  if (payNo && payNo.length > 5) return '支付宝'
  return '未支付'
}
// ⚠ 仅从 payNo 是否存在推断支付方式，无法区分微信/银行卡等
```

### 2.3 AI 聊天中的订单卡片（MessageContent.vue）

AI 助手通过 PARAM 事件接收 `orderInfo_*` 参数，渲染为订单卡片：

| 订单状态 | 卡片样式 | 操作按钮 |
|---|---|---|
| NOT_PAY | 橙色状态标签 + 课程名+价格+时间 | "立即支付" → 支付宝 |
| PAID/DONE | 绿色状态标签 + 课程名+价格+时间 | "查看详情" → 订单详情 |
| CANCELLED/REFUNDED | 灰色状态标签 + 课程名+价格+时间 | "查看详情" |
| REFUND_PENDING | 橙色状态标签 | 无操作 |
| ERROR | 红色主题 + errorMessage | "查看我的订单" → 订单列表 |

⚠ AI 聊天卡片使用 CANCELLED（双写 L）和 REFUND_PENDING，与后端实际返回的 CANCEL 和 REFUND_DONE 不一致。

### 2.4 路由与导航

| 路由 | 名称 | requiresAuth | 说明 |
|---|---|---|---|
| `/orders` | OrderList | true | 订单列表 |
| `/orders/:id` | OrderDetail | true | 订单详情 |
| `/checkout/:courseId` | Checkout | true | 结算页 |

Navbar "我的订单"链接位于已认证导航区域，需要登录。

### 2.5 类型定义

```typescript
// order.ts
interface Order {
  id: number
  orderNo: string
  goodsId: number          // ⚠ 后端字段名为 courseId
  courseId: number         // ⚠ 前端同时定义两个
  courseName: string
  courseCover: string
  totalAmount: number
  status: number | string  // ⚠ 联合类型，兼容数字和字符串状态
  createTime: string
  payTime: string
  payNo: string
  updateTime: string
  userId: number
}

interface OrderStatistics {
  totalOrders: number
  pendingOrders: number
  completedOrders: number
  refundedOrders: number
}

interface OrderSearchParams {
  page?: number
  pageSize?: number
  status?: string | number
}

interface PaymentRequest {
  orderNo: string
  totalAmount: number
}
```

⚠ `goodsId` 与 `courseId` 双字段命名：后端实体使用 `goodsId`（沿袭电商命名），前端 `Order` 接口同时定义两者，部分组件使用 `courseId`，部分使用 `goodsId`，存在不一致。

### 2.6 API 方法

```typescript
// user.ts
export const userApi = {
  createCourseOrder: (courseId: number) =>
    POST('/api/user/orders/create', { params: { courseId } }),
  getOrderDetail: (id: number) =>
    GET(`/api/user/orders/${id}`),
  getMyOrders: (params: OrderSearchParams) =>
    GET('/api/user/orders/page', params),
  getOrderByOrderNo: (orderNo: string) =>
    GET(`/api/user/orders/no/${orderNo}`),
  cancelOrder: (id: number) =>
    POST(`/api/user/orders/${id}/cancel`),
  payOrder: (id: number) =>
    POST(`/api/user/orders/${id}/pay`),  // ⚠ 未使用，实际走 paymentApi
}

// payment.ts
export const paymentApi = {
  alipayPay: (orderNo: string, totalAmount: number) => {
    // ⚠ 非 Axios，直接 window.location.href 重定向
    window.location.href = `http://192.168.88.130:8080/api/payment/pay?orderNo=${orderNo}&totalAmount=${totalAmount}`
  },
  alipayRefund: (orderNo: string, totalAmount: number) =>
    POST('/api/payment/refund', { orderNo, refundAmount: totalAmount }),
  alipayNotify: () => POST('/api/payment/notify'),  // ⚠ 前端未使用
}
```

⚠ `alipayPay` 硬编码后端 IP 地址 `192.168.88.130:8080`，无法适应生产环境。

---

## 三、后端执行流程

### 3.1 Sa-Token 认证架构

> Sa-Token 认证架构详见 [认证系统指南](../公共参考/认证系统指南.md) 及 [公共参考-认证与权限体系](../../公共参考/公共数据模型与基础配置.md#二认证与权限体系sa-token)

**三套 Controller，三种角色**：

```
请求进入
│
├── 用户端: OrdersUserController
│   ├── @SaCheckLogin + @SaCheckRole("用户")
│   └── 所有关键操作均验证订单归属(userId 匹配)
│
├── 管理端: OrdersAdminController
│   └── @SaCheckRole("管理员")
│
├── 教师端: OrdersTeacherController
│   └── @SaCheckRole("教师")
│       ⚠ 详情接口无归属校验（TODO 注释）
│
└── 支付端: PaymentController
    └── ⚠ 无认证注解（退款/支付接口对外暴露）
```

| 端点 | Interceptor | 注解 | 实际认证 |
|---|---|---|---|
| `/api/user/orders/*` | 拦截 | `@SaCheckLogin + @SaCheckRole("用户")` | **需登录+用户角色** |
| `/api/admin/orders/*` | 拦截 | `@SaCheckRole("管理员")` | **需登录+管理员角色** |
| `/api/teacher/orders/*` | 拦截 | `@SaCheckRole("教师")` | **需登录+教师角色** |
| `/api/payment/*` | ⚠ 未拦截 | ⚠ 无注解 | **无认证** |

### 3.2 我的订单列表（`GET /api/user/orders/page`）

```
请求参数: OrderQueryDTO { pageNum(default 1), pageSize(default 10), userId?, goodsId?, status?, orderNo? }
│
┌─ Sa-Token 校验: @SaCheckLogin + @SaCheckRole("用户")
│
┌─ 步骤1: 构建 DAO 查询对象
│  ├── Orders condition = new Orders()
│  ├── condition.setUserId(SaUtil.getLoginId())   ← 强制当前用户
│  └── condition.setStatus(status)                 ← 仅 status 传入
│  ⚠ goodsId 和 orderNo 从 DTO 接收但被静默忽略
│  ⚠ userId 从 DTO 接收但被 SaUtil.getLoginId() 覆盖
│
┌─ 步骤2: PageHelper 分页
│  └── PageHelper.startPage(pageNum, pageSize)
│
┌─ 步骤3: DAO 查询
│  └── ordersDao.queryByUserId(condition)
│      SQL: SELECT ... FROM orders
│           WHERE user_id = #{userId}
│           [AND status = #{status}]
│           ORDER BY create_time DESC
│      ⚠ 仅支持 userId + status 过滤，goodsId/orderNo 条件未生效
│
┌─ 步骤4: 实体→VO 转换
│  └── convertToVO() → 每条记录:
│      courseDao.queryById(goodsId) → 课程名+封面查找
│      ⚠ N+1 查询问题
│
└─ 返回 Result<PageInfo<OrderVO>>
    字段: id, orderNo, goodsId, courseName, courseCover,
          totalAmount, status, createTime, payTime, payNo
```

### 3.3 订单详情（`GET /api/user/orders/{id}`）

```
请求参数: id (path variable, min=1)
│
┌─ Sa-Token 校验
│
┌─ 步骤1: DAO 查询
│  └── ordersDao.queryById(id)
│
┌─ 步骤2: 归属校验
│  └── order.getUserId() != SaUtil.getLoginId() → MyException("无权查看")
│
┌─ 步骤3: 课程信息查找
│  └── courseDao.queryById(goodsId) → courseName + courseCover
│
└─ 返回 Result<OrderVO>
    含完整订单信息 + 课程名称 + 课程封面
```

### 3.4 创建课程订单（`POST /api/user/orders/create`）

```
请求参数: courseId (query parameter)
│
┌─ Sa-Token 校验
│
┌─ 步骤1: 调用 CoursePurchaseService.purchaseCourse(userId, courseId)
│   │
│   ├── 检查课程是否存在
│   ├── 检查是否已购买 → selectByUserIdAndCourseIdDone
│   │   SQL: SELECT ... FROM orders WHERE user_id=? AND goods_id=? AND status='DONE'
│   │   ⚠ 仅检查 DONE 状态，NOT_PAY 的未支付订单不阻止重复下单
│   │
│   ├── 检查是否有未支付订单 → selectByUserIdAndCourseIdNotPay
│   │   ├── 存在 → 直接返回已有订单（避免重复创建）
│   │   └── 不存在 → 创建新订单
│   │
│   ├── 生成订单号: "ORD" + System.currentTimeMillis() + random(4)
│   ├── 设置 status = "NOT_PAY"
│   ├── ordersDao.insert(orders)
│   └── 返回 OrderVO
│
└─ 返回 Result<OrderVO>
    ⚠ 创建订单不触发支付，需前端另行调用支付接口
```

### 3.5 取消订单（`POST /api/user/orders/{id}/cancel`）

```
请求参数: id (path variable)
│
┌─ Sa-Token 校验
│
┌─ 步骤1: 查询订单 + 归属校验
│
┌─ 步骤2: 状态校验
│  └── 仅 NOT_PAY 可取消 → 否则 MyException("只能取消待支付订单")
│
┌─ 步骤3: 更新状态
│  └── ordersDao.updateStatus(id, "CANCEL")
│      SQL: UPDATE orders SET status = 'CANCEL' WHERE id = #{id}
│
└─ 返回 Result<String>("取消成功")
```

### 3.6 支付订单（`POST /api/user/orders/{id}/pay`）

```
请求参数: id (path variable)
│
┌─ Sa-Token 校验
│
┌─ 步骤1: 查询订单 + 归属校验
│
┌─ 步骤2: 状态校验
│  └── 仅 NOT_PAY 可支付 → 否则 MyException("订单状态不正确")
│
┌─ 步骤3: ⚠ @Transactional 自调用失效
│  ├── this.payOrder(order) → 内部调用，事务不生效
│  └── payOrder 方法标注 @Transactional 但通过 this 调用绕过了 AOP 代理
│
┌─ 步骤4: 调用支付服务
│  └── 实际支付通过 PaymentController 单独完成（浏览器重定向）
│
└─ ⚠ 此端点仅标记"准备支付"，不执行实际扣款
```

### 3.7 按订单号查询（`GET /api/user/orders/no/{orderNo}`）

```
请求参数: orderNo (path variable)
│
┌─ Sa-Token 校验
│
├── ordersDao.queryByOrderNo(orderNo)
│   SQL: SELECT ... FROM orders WHERE order_no = #{orderNo}
│
├── 归属校验: order.userId != loginId → MyException("无权查看")
│
└── 返回 Result<OrderVO>
```

### 3.8 管理员订单列表（`GET /api/admin/orders/page`）

```
请求参数: OrderQueryDTO (pageNum, pageSize, userId?, goodsId?, status?, orderNo?)
│
┌─ Sa-Token 校验: @SaCheckRole("管理员")
│
┌─ 步骤1: 构建查询条件（完整支持所有字段）
│  ├── userId 非空 → condition.setUserId(userId)
│  ├── goodsId 非空 → condition.setGoodsId(goodsId)
│  ├── status 非空 → condition.setStatus(status)
│  └── orderNo 非空 → condition.setOrderNo(orderNo)
│  ⚠ 与用户端不同，管理员端支持全部筛选条件
│
┌─ 步骤2: DAO 查询
│  └── ordersDao.queryAllOrders(condition)
│      SQL: SELECT ... FROM orders
│           <where>
│             [AND user_id = #{userId}]
│             [AND goods_id = #{goodsId}]
│             [AND status = #{status}]
│             [AND order_no = #{orderNo}]
│           </where>
│           ORDER BY create_time DESC
│
└─ 返回 Result<PageInfo<OrderVO>>
```

### 3.9 管理员统计（`GET /api/admin/orders/statistics`）

```
│
┌─ Sa-Token 校验: @SaCheckRole("管理员")
│
├── ⚠ 6 次独立数据库查询：
│   1. ordersDao.getTotalOrderCount()        → 总订单数
│   2. ordersDao.getTotalRevenue()           → 总收入 (SUM WHERE status='DONE')
│   3. ordersDao.getTodayOrderCount()        → 今日订单数
│   4. ordersDao.getTodayRevenue()           → 今日收入
│   5. ordersDao.getPendingOrderCount()      → 待付款数
│   6. ordersDao.getRefundedOrderCount()     → 已退款数
│
└─ 返回 Result<OrderStatisticsVO>
    ⚠ 每次请求 6 次查询，无缓存，高频访问性能差
```

### 3.10 教师端订单查询

```
教师端三个查询均通过 INNER JOIN course 表实现教师归属过滤:

┌── 我的课程订单 (my-course-orders)
│   SQL: SELECT o.* FROM orders o
│        INNER JOIN course c ON o.goods_id = c.id
│        WHERE c.teacher_id = #{teacherId}
│        [AND o.status = #{status}]
│        ORDER BY o.create_time DESC
│
├── 我的收入趋势 (my-revenue)
│   SQL: SELECT DATE(o.create_time) as date, SUM(o.total_amount) as revenue
│        FROM orders o INNER JOIN course c ON o.goods_id = c.id
│        WHERE c.teacher_id = #{teacherId} AND o.status = 'DONE'
│        ⚠ DATE() 函数作用于 VARCHAR 列，依赖 MySQL 隐式类型转换
│        ⚠ 应使用 STR_TO_DATE() 或改为 DATE/TIMESTAMP 列类型
│
├── 热门课程排名 (top-courses)
│   SQL: SELECT c.title, COUNT(*) as order_count, SUM(o.total_amount) as revenue
│        FROM orders o INNER JOIN course c ON o.goods_id = c.id
│        WHERE c.teacher_id = #{teacherId} AND o.status = 'DONE'
│        GROUP BY c.id, c.title ORDER BY order_count DESC LIMIT #{limit}
│
└── 订单详情 ({id})
    ├── ordersDao.queryById(id)
    ⚠ 无归属校验 — 教师可查看任意订单详情（TODO 注释）
```

### 3.11 支付宝支付与回调

```
支付流程:
│
├── 前端: window.location.href → GET /api/payment/pay?orderNo=xxx&totalAmount=xxx
│   └── ⚠ 无认证，GET 请求传递金额，可被篡改
│
├── PaymentController.pay():
│   ├── 构建 AlipayClient (沙箱配置)
│   ├── AlipayTradePagePayRequest
│   ├── setReturnUrl → 前端同步回调页
│   ├── setNotifyUrl → /api/payment/notify (异步通知)
│   └── 返回支付宝收银台 HTML
│
├── 支付宝异步通知 → POST /api/payment/notify
│   ├── 验签: AlipaySignature.rsaCheckV1()
│   ├── 提取 trade_status + out_trade_no + trade_no
│   ├── trade_status == "TRADE_SUCCESS" →
│   │   └── handlePaymentSuccess(orderNo, tradeNo)
│   │       ├── 幂等检查: 订单状态非 NOT_PAY → 跳过
│   │       ├── updateStatus(orderNo, "DONE")     → 更新订单状态
│   │       ├── updatePayInfo(orderNo, tradeNo)    → 设置 payNo + payTime
│   │       ├── addToMyCourse(userId, courseId)     → ⚠ 异常被吞没
│   │       └── updateCourseSales(courseId)        → ⚠ 异常被吞没
│   │           ⚠ buyCount 双重递增 Bug: updateSales 中 buyCount+1，
│   │              同时 CoursePurchaseServiceImpl.purchaseCourse 也会 buyCount+1
│   │
│   └── 返回 "success"（支付宝要求）
│
├── 退款 → POST /api/payment/refund
│   ├── ⚠ 无认证注解，任何人可调用
│   ├── AlipayTradeRefundRequest
│   ├── refundAmount = 订单金额（不支持部分退款）
│   └── 退款成功 → updateStatus(orderNo, "REFUND_DONE")
│
└── payTime 写入 Bug:
    ├── handlePaymentSuccess 中: new Date().toString()
    └── ⚠ Date.toString() 格式为 "Mon Jan 01 00:00:00 CST 2024"
        非标准日期格式，前端 new Date() 解析可能失败或时区偏移
        应使用 SimpleDateFormat("yyyy-MM-dd HH:mm:ss")
```

### 3.12 数据库表结构

> 数据库表结构详见 [数据库表结构汇总](../../公共参考/数据库表结构汇总.md)（orders）

⚠ `create_time` 和 `pay_time` 使用 VARCHAR 类型存储，依赖 Java 端 `new Date().toString()` 写入，导致：
- MySQL 日期函数（DATE/STR_TO_DATE）需要隐式转换
- 教师收入趋势查询 `DATE(o.create_time)` 依赖隐式转换
- 排序 `ORDER BY create_time DESC` 是字符串排序而非时间排序
- 时区信息不可控（CST 歧义：中国标准时间/中央标准时间）

**索引**：user_id, status, order_no(UNIQUE), goods_id, (user_id + status) 联合索引

### 3.13 MyBatis SQL 汇总

| SQL 方法 | WHERE 条件 | 排序 | 说明 |
|---|---|---|---|
| queryByUserId | user_id + [status] | create_time DESC | 用户订单列表 |
| queryAllOrders | 全条件动态 | create_time DESC | 管理员订单列表 |
| queryByTeacherId | INNER JOIN course + teacher_id + [status] | create_time DESC | 教师订单列表 |
| queryById | id | — | 订单详情 |
| queryByOrderNo | order_no | — | 按订单号查询 |
| selectByUserIdAndCourseIdDone | user_id + goods_id + status='DONE' | — | 已购买检查 |
| selectByUserIdAndCourseIdNotPay | user_id + goods_id + status='NOT_PAY' | — | 未支付订单检查 |
| insert | — | — | 创建订单 |
| updateStatus | id + status | — | 状态更新 |
| updatePayInfo | order_no + payNo + payTime | — | 支付信息更新 |
| getTotalOrderCount | — | — | 总订单数 |
| getTotalRevenue | status='DONE' | — | 总收入 |
| getTodayOrderCount | DATE(create_time)=CURDATE() | — | 今日订单 |
| getTodayRevenue | DATE(create_time)=CURDATE() + status='DONE' | — | 今日收入 |
| getPendingOrderCount | status='NOT_PAY' | — | 待付款数 |
| getRefundedOrderCount | status='REFUND_DONE' | — | 已退款数 |
| getTeacherRevenueTrend | teacher_id + DATE(create_time) GROUP BY date | — | ⚠ DATE() on VARCHAR |
| getTopCoursesByTeacherId | teacher_id + status='DONE' GROUP BY course | order_count DESC | 热门课程 |
| deleteById | id | — | 硬删除 |

### 3.14 DTO 校验规则

| DTO | 字段 | 校验 |
|---|---|---|
| OrderCreateDTO | courseId | `@NotNull @Positive` |
| | totalAmount | `@NotNull @DecimalMin("0.01")` |
| | ⚠ 此 DTO 已定义但从未使用 | |
| OrderQueryDTO | pageNum | `@Min(1)` default 1 |
| | pageSize | `@Min(1) @Max(100)` default 10 |
| | userId | `@Positive`（用户端被覆盖） |
| | goodsId | `@Positive`（用户端被忽略） |
| | status | `@Size(max=20)` |
| | orderNo | `@Size(max=50)`（用户端被忽略） |

---

## 四、前后端交互时序

### 订单列表加载

```
OrderList.vue onMounted
│
└── GET /api/user/orders/page?page=1&pageSize=10&status=
    ├── Sa-Token 校验: 登录 + 用户角色
    ├── ordersDao.queryByUserId(userId, status)
    ├── N+1: 每条查 courseDao.queryById(goodsId)
    └── 返回 PageInfo<OrderVO>
    │
    ├── 渲染订单卡片列表
    └── calculateStatistics()  ⚠ 仅当前页数据
```

### 支付流程（完整时序）

```
用户点击"去支付"
│
├── 前端: paymentApi.alipayPay(orderNo, totalAmount)
│   └── window.location.href = http://192.168.88.130:8080/api/payment/pay?...
│
├── PaymentController.pay()
│   ├── AlipayTradePagePayRequest
│   ├── sdkExecute() → 生成支付宝收银台 HTML
│   └── 返回 HTML（浏览器自动渲染跳转）
│
├── 支付宝收银台 → 用户完成支付
│
├── 支付宝异步通知 → POST /api/payment/notify
│   ├── 验签
│   ├── handlePaymentSuccess(orderNo, tradeNo)
│   │   ├── 幂等检查
│   │   ├── updateStatus → "DONE"
│   │   ├── updatePayInfo → payNo + payTime(Date.toString())
│   │   ├── addToMyCourse → ⚠ 异常被吞
│   │   └── updateCourseSales → ⚠ buyCount 双重递增
│   └── 返回 "success"
│
├── 支付宝同步回调 → 前端 return_url
│   ├── 前端: ElMessage.success('支付成功')  ← ⚠ 页面已导航，提示不可见
│   └── router.push('/orders')
│
└── 用户在订单列表看到状态更新
```

### 退款流程

```
用户点击"申请退款"
│
├── OrderList.vue:
│   ├── paymentApi.alipayRefund(orderNo, totalAmount)
│   │   → POST /api/payment/refund { orderNo, refundAmount }
│   ├── ⚠ 无认证，任何人可调用
│   ├── PaymentController.refund():
│   │   ├── AlipayTradeRefundRequest
│   │   ├── 退款成功 → updateStatus(orderNo, "REFUND_DONE")
│   │   └── 退款失败 → 返回错误信息
│   └── ElMessage.success('退款申请已提交')
│
├── OrderDetail.vue:
│   ├── ⚠ 假退款 — 仅显示成功提示，未调用 API
│   └── ElMessage.success('退款申请已提交')
│
└── 状态不一致: OrderList 退款真实，OrderDetail 退款虚假
```

---

## 五、已知问题与风险点

> 跨模块系统级问题见 [公共参考-系统级已知问题与风险清单](../../公共参考/系统级已知问题与风险清单.md)，此处仅列本模块独有问题。

| # | 问题 | 严重度 | 说明 |
|---|---|---|---|
| 1 | **OrderDetail 退款为假实现** | 高 | 仅显示成功提示但未调用退款 API，用户以为已退款但实际未执行 |
| 2 | **退款按钮可见性不一致** | 中 | OrderList 仅 DONE 可退，OrderDetail DONE+PAID 可退 |
| 3 | **统计仅基于当前页** | 中 | `calculateStatistics()` 仅统计当前页订单数量，用户看到的数字不反映真实全局数据 |
| 4 | **管理员统计 6 次 DB 查询** | 中 | 每次请求 6 个独立查询，无缓存，应合并或缓存 |
| 5 | **payTime 使用 Date.toString()** | 中 | 格式为 "Mon Jan 01 00:00:00 CST 2024"，非标准日期格式，前端解析可能失败或时区偏移 |
| 6 | **教师详情接口无归属校验** | 高 | 教师可查看任意用户订单详情，存在信息泄露风险 |
| 7 | **用户端筛选条件被忽略** | 中 | OrderQueryDTO 的 goodsId/orderNo 在用户端查询中被静默忽略 |
| 8 | **OrderConstants 为空** | 中 | 订单状态常量类无实际内容，所有状态字符串硬编码分散 |
| 9 | **OrderCreateDTO 未使用** | 低 | DTO 已定义但创建订单未使用，参数直接从 query 取 |
| 10 | **支付成功提示不可见** | 低 | 支付宝同步回调后 `ElMessage.success` 执行，但页面已导航到订单列表，用户看不到 |
| 11 | **goToCourse 跳转错误** | 低 | 订单详情"查看课程"跳转 CourseDetail 而非 CourseLearn，已购课程应进入学习页 |
| 12 | **updateTime=createTime Bug** | 低 | OrderDetail 中将 updateTime 硬编码为 createTime |
| 13 | **goodsId/courseId 双命名** | 低 | 后端实体用 goodsId，前端同时定义 goodsId 和 courseId，组件间使用不一致 |
| 14 | **无订单超时自动取消** | 中 | NOT_PAY 订单无定时清理机制，可能无限期占用 |
| 15 | **退款仅支持全额** | 低 | refundAmount = totalAmount，不支持部分退款 |
| 16 | **重复下单检查不完整** | 中 | 仅检查 DONE 和 NOT_PAY 状态，CANCEL 状态的订单允许重复购买同一课程 |
