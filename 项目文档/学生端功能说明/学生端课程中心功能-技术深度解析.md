# 学生端课程中心功能 — 技术深度解析

## 一、功能概述

学生端课程中心是码趣星球平台的核心模块，涵盖课程浏览、搜索、详情、学习、购买/加入及评论等全链路功能。前端通过 `courseApi.ts` 智能切换 `publicApi`/`userApi`，实现免登录浏览与登录后个性化体验的无缝衔接。

### 功能清单

| 功能 | 前端页面/组件 | 后端端点 | 认证要求 |
|---|---|---|---|
| 课程列表（分页） | CourseList.vue | `GET /api/public/course/page` | 否 |
| 课程搜索 | CourseList.vue | `GET /api/public/course/page`（title 参数） | 否 |
| 分类筛选 | CourseList.vue | `GET /api/public/course/page`（subjectId） | 否 |
| 免费课程 | CourseList.vue | `GET /api/public/course/free` | 否 |
| 热门课程 | CourseList.vue 侧栏 | `GET /api/public/course/hot` | 否 |
| 课程详情（公共） | CourseDetail.vue | `GET /api/public/course/{id}` | 否 |
| 课程详情（个性化） | CourseDetail.vue | `GET /api/user/course/{id}` | `@SaCheckRole("用户")` |
| 课程分类树 | CourseList.vue 侧栏 | `GET /api/public/course-subject/tree` | 否 |
| 课程学习 | CourseLearn.vue | `GET /api/user/course/{id}/can-study` + `GET /api/user/course/{id}/detail` | `@SaCheckRole("用户")` |
| 章节结构 | CourseChapters.vue | `GET /api/public/chapter-section/course/{courseId}/structure` | 否 |
| 加入课程 | CourseDetail.vue | `POST /api/user/my-course/join` | `@SaCheckRole("用户")` |
| 我的课程列表 | MyCourses.vue | `GET /api/user/my-course/list` | `@SaCheckRole("用户")` |
| 我的课程详情 | MyCourses.vue | `GET /api/user/my-course/{id}` | `@SaCheckRole("用户")` |
| 学习进度更新 | CourseLearn.vue | `PUT /api/user/my-course/progress` | `@SaCheckRole("用户")` |
| 退出课程 | MyCourses.vue | `POST /api/user/my-course/{id}/quit` | `@SaCheckRole("用户")` |
| 课程评论 | CourseComments.vue | `GET /api/public/comment/course/{courseId}` | 否（列表） |
| 发表评论 | CourseComments.vue | `POST /api/user/comment` | `@SaCheckLogin` |
| 记录浏览 | CourseDetail.vue | `POST /api/user/course/{id}/view` | `@SaCheckRole("用户")` |
| AccessModal | AccessModal.vue | — | 前端组件，引导登录/购买/加入 |

### 三级访问模型

课程内容按层级实施不同的访问控制：

| 层级 | 条件 | 可访问内容 | 不可访问时 |
|---|---|---|---|
| **免费试看** | 无需任何条件 | 课程基本信息 + 免费小节内容 | 无 |
| **登录用户** | 已登录 + 用户角色 | 个性化信息（购买/加入状态） | 弹出 AccessModal（登录类型） |
| **已购/已加入** | 完成购买或加入 | 全部课程内容 + 学习进度 | 弹出 AccessModal（购买/加入类型） |

### courseApi.ts 双 API 智能切换

前端课程模块的核心设计 — 根据 `isAuthenticated` 状态自动选择 `publicApi` 或 `userApi`，并在 `userApi` 失败时 fallback 回 `publicApi`：

```typescript
// courseApi.ts 核心模式
const getCourseDetail = async (id: number) => {
  if (isAuthenticated.value) {
    try {
      return await userApi.getCourseDetail(id)   // 个性化详情
    } catch {
      return await publicApi.getCourseDetail(id)  // fallback
    }
  }
  return await publicApi.getCourseDetail(id)      // 未登录
}
```

**适用场景**：课程详情、课程列表（4 分支选择）均使用此模式。章节结构、评论列表等纯公共数据直接走 `publicApi`。

---

## 二、前端关键逻辑

### 2.1 课程列表页（CourseList.vue）

#### 页面布局

```
┌───────────────────────────────────────────────────────┐
│ Navbar                                                 │
├──────────────────────┬────────────────────────────────┤
│ 侧栏（280px）        │ 主内容区                       │
│ ┌──────────────────┐ │ ┌────────────────────────────┐ │
│ │ 课程分类树       │ │ │ 搜索/筛选栏               │ │
│ │ (两级手风琴)     │ │ │ [搜索框] [分类下拉] [免费] │ │
│ └──────────────────┘ │ ├────────────────────────────┤ │
│ ┌──────────────────┐ │ │ 课程卡片网格（3列）        │ │
│ │ 🔥 热门课程      │ │ │ ┌──────┐ ┌──────┐ ┌────┐ │ │
│ │ Top 5            │ │ │ │封面  │ │封面  │ │    │ │ │
│ └──────────────────┘ │ │ │标题  │ │标题  │ │    │ │ │
│                      │ │ │描述  │ │描述  │ │    │ │ │
│                      │ │ │价格  │ │价格  │ │    │ │ │
│                      │ │ └──────┘ └──────┘ └────┘ │ │
│                      │ ├────────────────────────────┤ │
│                      │ │ 分页控件                   │ │
│                      │ └────────────────────────────┘ │
└──────────────────────┴────────────────────────────────┘
```

#### 数据状态

```typescript
const courses = ref<Course[]>([])
const total = ref(0)
const loading = ref(false)
const categories = ref<CourseSubject[]>([])
const hotCourses = ref<Course[]>([])
const searchParams = reactive({
  page: 1, size: 12, title: '', subjectId: undefined, freeOnly: false
})
```

#### 数据加载流程

```
onMounted → URL query 参数初始化 → Promise.all([getCourses(), getCategories(), getHotCourses()])
│
├── getCourses()
│   ├── 4 分支 API 选择:
│   │   ├── freeOnly + 登录  → userApi.getFreeCourses(params)
│   │   ├── freeOnly + 未登录 → publicApi.getFreeCourses(params)
│   │   ├── 登录              → userApi.getCourseList(params)
│   │   └── 未登录            → publicApi.getCourseList(params)
│   ├── 多重 fallback 解包: response.data?.list || response.list || response.records || []
│   └── 赋值 courses + total
│
├── getCategories()
│   ├── publicApi.getCourseSubjectTree()
│   ├── 仅取 parentId===0 的顶级分类
│   └── 赋值 categories → 渲染侧栏分类树
│
└── getHotCourses()
    ├── publicApi.getHotCourses(5)
    └── 赋值 hotCourses → 渲染侧栏热门区
```

#### 搜索与筛选逻辑

| 操作 | 触发方式 | 逻辑 |
|---|---|---|
| 关键词搜索 | 输入回车 / 搜索按钮 | `searchParams.page=1` → `getCourses()` |
| 分类侧栏 | 点击分类节点 | `subjectId=节点id` → `getCourses()` |
| 分类下拉 | select change | `handleSearch()` |
| 免费筛选 | checkbox toggle | `freeOnly=true/false` → `getCourses()` |
| 分页翻页 | el-pagination | 更新 page → `getCourses()` |
| 每页条数 | el-pagination sizes | 更新 size + page=1 → `getCourses()` |
| 清除筛选 | "清除筛选"按钮 | 重置全部参数 → `getCourses()` |

#### URL Query 初始化

```
路由进入 → 读取 route.query
├── subjectId → 设置 searchParams.subjectId + 展开对应分类
├── title → 设置 searchParams.title
└── 初始化完成后触发首次查询
```

支持从首页分类卡片点击跳转（带 `subjectId`）和搜索结果跳转（带 `title`）。

### 2.2 课程详情页（CourseDetail.vue）

#### 页面布局

```
┌───────────────────────────────────────────────────────────┐
│ 面包屑: 首页 > 课程列表 > 课程名称                          │
├──────────────────────────────┬────────────────────────────┤
│ 主内容区（左 2/3）            │ 侧栏信息区（右 1/3）       │
│                              │                            │
│ 课程封面（大图）              │ 价格 + 购买/加入按钮       │
│ 课程标题 + 分类标签           │ 课程信息（课时/时长/学员）  │
│ 教师信息（头像+名称）         │ AccessModal 触发区        │
│ 课程描述                     │                            │
│ 课程章节（CourseChapters）    │                            │
│ 课程评论（CourseComments）    │                            │
├──────────────────────────────┴────────────────────────────┤
│ 相关推荐课程（底部）                                        │
└───────────────────────────────────────────────────────────┘
```

#### 数据获取流程

```
onMounted → getCourseDetail()
│
├── 智能API调用:
│   ├── isAuthenticated → userApi.getCourseDetail(id)
│   │   → GET /api/user/course/{id}
│   │   → 返回 CourseDetailVO（含 isPurchased/isJoined/canStudy 等个性化字段）
│   │
│   └── 未登录 → publicApi.getCourseDetail(id)
│       → GET /api/public/course/{id}
│       → 返回 CourseListVO（基础字段，无个性化信息）
│
├── 成功后:
│   ├── getChapters() → GET /api/public/chapter-section/course/{courseId}/structure
│   ├── isAuthenticated → recordView(id) → POST /api/user/course/{id}/view
│   └── getRelatedCourses() → 从分类或热门中取推荐
│
└── 失败 → 404/不存在处理
```

#### 个性化状态字段（仅 userApi 返回）

| 字段 | 类型 | 含义 |
|---|---|---|
| `isPurchased` | boolean | 是否已购买（有完成订单） |
| `isJoined` | boolean | 是否已加入 MyCourse |
| `canStudy` | boolean | 是否可以学习（免费=可，付费=需已购） |
| `myCourseId` | number | MyCourse 记录 ID（已加入时） |

#### 购买/加入按钮状态

| 状态 | 按钮文案 | 行为 |
|---|---|---|
| 免费课程 + 未加入 | "免费加入" | `handleJoinCourse()` → `myCourseApi.joinCourse(courseId)` |
| 免费课程 + 已加入 | "继续学习" | `router.push('/courses/{id}/learn')` |
| 付费课程 + 未购买 | "立即购买 ¥{price}" | `handlePurchase()` → 跳转支付 |
| 付费课程 + 已购买 + 未加入 | "开始学习" | `handleJoinCourse()` |
| 付费课程 + 已加入 | "继续学习" | `router.push('/courses/{id}/learn')` |

#### 章节组件（CourseChapters.vue）

```
┌────────────────────────────────────────┐
│ ▼ 第一章：Python入门基础              │  ← 手风琴展开/收起
│   ├── 1.1 课程介绍              [免费] │  ← 小节标签
│   ├── 1.2 环境搭建              [免费] │
│   ├── 1.3 变量与数据类型        [锁定] │  ← 付费锁定标识
│   └── 1.4 运算符                [锁定] │
│ ▶ 第二章：流程控制                     │  ← 收起状态
│ ▶ 第三章：函数与模块                     │
└────────────────────────────────────────┘
```

- **免费小节**：显示"免费"标签，点击直接跳转学习
- **锁定小节**：未购买/未加入时显示锁图标，点击触发 AccessModal
- **已购/已加入**：所有小节可点击进入学习

数据来源：`publicApi.getCourseStructure(courseId)` 返回两级树（Chapter → Section），每节含 `isFree` 字段。

### 2.3 课程学习页（CourseLearn.vue）

#### 页面布局

```
┌───────────────────────────────────────────────────────────┐
│ 视频播放区（HTML5 video）                                   │
│ ┌─────────────────────────────────────────────────────────┐│
│ │ <video> 播放器                                          ││
│ │ 自动播放 + 控件 + 画中画                                ││
│ └─────────────────────────────────────────────────────────┘│
├───────────────────────────────────────────────────────────┤
│ 章节列表（当前章展开，其余收起）                              │
│ ┌─────────────────────────────────────────────────────────┐│
│ │ ▶ 第一章        │ ▼ 第二章（当前）        │ ▶ 第三章   ││
│ │                 │   ● 2.1（播放中）       │            ││
│ │                 │   ○ 2.2                 │            ││
│ │                 │   ○ 2.3                 │            ││
│ └─────────────────────────────────────────────────────────┘│
├───────────────────────────────────────────────────────────┤
│ 底部导航：上一节 | 下一节                                   │
└───────────────────────────────────────────────────────────┘
```

#### 三级访问验证流程

```
onMounted → 验证访问权限
│
├── 步骤1: 检查登录状态
│   └── 未登录 → 弹出 AccessModal(type: 'login')
│       └── 登录后 → 继续验证
│
├── 步骤2: 检查学习权限
│   ├── userApi.canStudyCourse(courseId)
│   │   → GET /api/user/course/{id}/can-study
│   │   → 返回 { canStudy: boolean, reason?: string }
│   │
│   ├── canStudy=false, 付费课程 → AccessModal(type: 'purchase')
│   └── canStudy=false, 免费课程 → AccessModal(type: 'join')
│
├── 步骤3: 自动加入（免费课程）
│   ├── 免费课程 + 未加入 → myCourseApi.joinCourse(courseId)
│   │   → POST /api/user/my-course/join
│   │   → 自动加入，状态设为"学习中"(1)
│   └── 加入成功 → 继续加载
│
└── 步骤4: 加载课程详情 + 章节结构
    ├── userApi.getCourseDetail(courseId) → 含个性化字段
    ├── publicApi.getCourseStructure(courseId) → 章节树
    └── 设置当前播放小节 → 自动播放视频
```

#### 视频播放

```typescript
// HTML5 <video> 元素
<video
  ref="videoRef"
  :src="currentSection.videoUrl"
  controls
  autoplay
  allowfullscreen
  preload="metadata"
/>
```

- **自动播放**：`autoplay` 属性，首次需用户交互后浏览器才允许
- **画中画**：`allowfullscreen` 支持
- **MIME 类型**：依赖浏览器自动识别，无服务端强制类型
- **进度追踪**：`timeupdate` 事件可记录学习进度

#### 小节切换

```
点击新小节 → handleSectionChange(section)
├── 检查访问权限:
│   ├── section.isFree → 直接播放
│   ├── isJoined/isPurchased → 直接播放
│   └── 锁定小节 → AccessModal
│
├── 更新 video src → 自动播放
│
└── 可选: 记录学习进度
    └── myCourseApi.updateProgress(myCourseId, { currentSectionId, progress })
```

### 2.4 AccessModal 组件（AccessModal.vue）

三种弹出类型，Promise 化调用模式：

```typescript
// 调用方式
const accessModal = ref()

// 登录引导
accessModal.value.show('login').then(() => { /* 登录成功回调 */ })

// 购买引导
accessModal.value.show('purchase', { courseId, price, title })

// 加入引导
accessModal.value.show('join', { courseId })
```

#### 三种类型 UI

| 类型 | 标题 | 内容 | 操作 |
|---|---|---|---|
| `login` | "请先登录" | 登录提示 + 功能说明 | "去登录" → 跳转 `/auth/login` |
| `purchase` | "购买课程" | 课程名 + 价格 + 支付说明 | "立即购买" → 跳转支付 |
| `join` | "加入课程" | 课程名 + 免费说明 | "立即加入" → `joinCourse()` API |

**Promise 设计**：`show()` 返回 Promise，用户完成操作后 resolve，关闭时 reject。调用方可在 `.then()` 中执行后续逻辑（如刷新详情、开始播放）。

### 2.5 课程评论组件（CourseComments.vue）

#### 数据加载

```
评论列表: GET /api/public/comment/course/{courseId}?page=1&size=10
├── 免登录查看
├── 返回 PageInfo<CommentVO>
└── 支持分页

发表评论: POST /api/user/comment
├── 需登录 (@SaCheckLogin)
├── 校验: 一门课程仅能评论一次
├── 校验: 必须已加入课程
└── 内容: { courseId, content, rating(1-5) }
```

#### 评论展示

```
┌──────────────────────────────────────────┐
│ 综合评分: ⭐ 4.5  (X条评论)              │
├──────────────────────────────────────────┤
│ ┌─ 评论项 ─────────────────────────────┐ │
│ │ [头像] 用户名   ⭐⭐⭐⭐  2024-01-01 │ │
│ │ 评论内容...                         │ │
│ └─────────────────────────────────────┘ │
│ ...                                      │
├──────────────────────────────────────────┤
│ 发表评论（需登录）                        │
│ [评分选择] [内容输入] [提交]             │
└──────────────────────────────────────────┘
```

#### 评论排序

| 排序方式 | SQL 排序 |
|---|---|
| 最新 | `create_time DESC` |
| 最高评分 | `rating DESC` |
| 最低评分 | `rating ASC` |

### 2.6 我的课程页（MyCourses.vue）

#### 三标签视图

| 标签 | 筛选条件 | myCourse status |
|---|---|---|
| 全部 | 无 | — |
| 学习中 | `status === 1` | 1 |
| 已完成 | `status === 2` | 2 |

数据加载：`userApi.getMyCourseList({ status? })` → `GET /api/user/my-course/list`

#### 课程卡片操作

| 状态 | 操作按钮 | 行为 |
|---|---|---|
| 已加入(0) | "开始学习" | `router.push('/courses/{courseId}/learn')` |
| 学习中(1) | "继续学习" | 同上 |
| 学习中(1) | "退出课程" | `quitCourse()` → 确认弹窗 → `POST /api/user/my-course/{id}/quit` |
| 已完成(2) | "查看课程" | 同上 |

### 2.7 类型定义

```typescript
interface Course {
  id: number; title: string; description: string; cover: string
  price: number; subjectId: number; teacherId: number; teacherName: string
  lessonNum: number; durationSum: number; buyCount: number; viewCount: number
  status: number; publishTime: string; createTime: string; updateTime: string
}

interface CourseDetail extends Course {
  isPurchased?: boolean; isJoined?: boolean; canStudy?: boolean
  myCourseId?: number; categoryName?: string
}

interface Chapter {
  id: number; courseId: number; title: string; sort: number
  sections: Section[]
}

interface Section {
  id: number; chapterId: number; title: string; sort: number
  videoUrl: string; isFree: boolean; duration: number
}

interface Comment {
  id: number; courseId: number; userId: number; username: string
  avatar: string; content: string; rating: number; createTime: string
}

interface MyCourse {
  id: number; courseId: number; userId: number; status: number
  currentSectionId: number; progress: number; joinTime: string
  lastStudyTime: string; course: Course
}
```

---

## 三、后端执行流程

### 3.1 认证架构

Sa-Token 拦截器（WebConfig.java）对课程相关端点的认证策略：

| 控制器 | 路径前缀 | Sa-Token 注解 | 实际认证 |
|---|---|---|---|
| CoursePublicController | `/api/public/course` | 无 | **免登录** |
| ChapterSectionPublicController | `/api/public/chapter-section` | 无 | **免登录** |
| CourseCommentPublicController | `/api/public/comment` | 无 | **免登录** |
| CourseSubjectPublicController | `/api/public/course-subject` | 无 | **免登录** |
| CourseUserController | `/api/user/course` | `@SaCheckRole("用户")` | **需登录+用户角色** |
| MyCourseController | `/api/user/my-course` | `@SaCheckRole("用户")` | **需登录+用户角色** |
| CourseCommentUserController | `/api/user/comment` | `@SaCheckLogin` | **需登录** |

### 3.2 课程列表（`GET /api/public/course/page`）

```
请求参数: PublishedCourseQueryDTO { title?, subjectId?, page(1), size(10) }
│
┌─ 步骤1: 强制设置 status=1（仅已发布）
│
┌─ 步骤2: PageHelper 分页
│  └── PageHelper.startPage(page, size)
│
┌─ 步骤3: DAO 查询
│  └── courseDao.queryAllByLimit(condition)
│      SQL: SELECT ... FROM course
│           WHERE status = 1
│           [AND title LIKE CONCAT('%',#{title},'%')]    ← 仅搜标题
│           [AND subject_id = #{subjectId}]               ← 精确匹配
│           ORDER BY create_time DESC
│
┌─ 步骤4: 填充 teacherName（⚠ N+1 查询）
│  └── 遍历 → userDao.queryById(teacherId) → teacherName
│
└─ 返回 Result<PageInfo<CourseListVO>>
    字段: id, title, cover, subjectId, teacherId, teacherName, price,
          lessonNum, durationSum, status, buyCount, viewCount, publishTime,
          createTime, updateTime
    ⚠ 不含 description（列表页不展示长文本）
```

### 3.3 热门课程（`GET /api/public/course/hot`）

```
请求参数: limit（默认10，范围1-100）
│
┌─ 步骤1: 查询所有已发布课程
│  ├── 构造 Course 条件: status=1
│  ├── courseDao.queryAllByLimit(condition)
│  │   SQL: SELECT ... FROM course WHERE status = 1 ORDER BY create_time DESC
│  └── ⚠ 加载全部已发布课程到内存
│
┌─ 步骤2: 内存排序
│  └── 按 viewCount 降序排序
│
┌─ 步骤3: 截取前 limit 条
│
┌─ 步骤4: 填充 teacherName（⚠ N+1 查询）
│
└─ 返回 Result<List<CourseListVO>>
```

**性能风险**：将全部已发布课程加载到内存排序，课程量大时内存占用和查询耗时显著增加。应改为 `SQL ORDER BY view_count DESC LIMIT #{limit}`。

### 3.4 免费课程（`GET /api/public/course/free`）

```
请求参数: PublishedCourseQueryDTO（同列表）
│
├── 额外条件: price = BigDecimal.ZERO（精确匹配）
│
├── SQL: ... AND price = 0
│
└── 其余流程同列表（PageHelper + N+1 teacherName）
```

### 3.5 课程详情 — 公共（`GET /api/public/course/{id}`）

```
请求参数: id (path variable)
│
┌─ 步骤1: DAO 查询
│  └── courseDao.queryById(id)
│      SQL: SELECT Base_Column_List FROM course WHERE id = #{id}
│
┌─ 步骤2: 状态检查
│  └── status != 1 → "课程不存在或未发布"
│
┌─ 步骤3: 填充 teacherName + categoryName
│  ├── userDao.queryById(teacherId) → teacherName
│  └── courseSubjectDao.queryById(subjectId) → categoryName
│
└─ 返回 Result<CourseDetailVO>
    字段: id, title, description, cover, subjectId, categoryName,
          teacherId, teacherName, price, lessonNum, durationSum,
          buyCount, viewCount, status, publishTime, createTime, updateTime
    ⚠ 不含 isPurchased/isJoined/canStudy（公共接口无个性化）
```

### 3.6 课程详情 — 个性化（`GET /api/user/course/{id}`）

```
请求参数: id (path variable)
│
┌─ Sa-Token 校验: @SaCheckRole("用户") → userId
│
┌─ 步骤1: 获取课程基础信息
│  └── courseDao.queryById(id) + status 检查
│
┌─ 步骤2: 填充 teacherName + categoryName
│
┌─ 步骤3: 查询购买状态
│  ├── orderService.getLatestOrderByUserAndCourse(userId, courseId)
│  │   SQL: SELECT ... FROM orders WHERE user_id=#{userId}
│  │        AND course_id=#{courseId} AND status='PAID'
│  │        ORDER BY create_time DESC LIMIT 1
│  └── isPurchased = (order != null)
│
┌─ 步骤4: 查询加入状态
│  ├── myCourseDao.selectByUserAndCourse(userId, courseId)
│  │   SQL: SELECT ... FROM my_course WHERE user_id=#{userId}
│  │        AND course_id=#{courseId} LIMIT 1
│  └── isJoined = (myCourse != null), myCourseId = myCourse.id
│
┌─ 步骤5: 判断学习权限
│  ├── 免费课程 → canStudy = true
│  └── 付费课程 → canStudy = isPurchased
│
└─ 返回 Result<CourseDetailVO>
    字段: 基础字段 + isPurchased, isJoined, canStudy, myCourseId
```

### 3.7 浏览记录（`POST /api/user/course/{id}/view`）

```
│
┌─ Sa-Token 校验: @SaCheckRole("用户")
│
┌─ 步骤1: 查询课程
│  └── courseDao.queryById(id) + status 检查
│
┌─ 步骤2: 递增浏览量（⚠ 非原子操作）
│  ├── int currentCount = course.getViewCount()
│  ├── course.setViewCount(currentCount + 1)
│  └── courseDao.update(course)
│       SQL: UPDATE course SET view_count = #{viewCount} WHERE id = #{id}
│
└─ 返回 Result.success("浏览记录已更新")
```

**并发风险**：读-改-写非原子操作，两个并发请求可能读到相同的 `viewCount`，导致最终仅 +1 而非 +2。应改为 `SQL: UPDATE course SET view_count = view_count + 1 WHERE id = #{id}`。

### 3.8 学习权限检查（`GET /api/user/course/{id}/can-study`）

```
│
┌─ Sa-Token 校验: @SaCheckRole("用户") → userId
│
┌─ 步骤1: 查询课程
│  └── courseDao.queryById(id) + status 检查
│
┌─ 步骤2: 判断权限
│  ├── price == 0 (免费) → canStudy = true
│  └── price > 0 (付费) → 检查是否有已完成订单
│      ├── orderService.getCompletedOrderByUserAndCourse(userId, courseId)
│      │   SQL: SELECT ... FROM orders WHERE user_id=#{userId}
│      │        AND course_id=#{courseId} AND status='PAID'
│      │   canStudy = (order != null)
│      └── 无订单 → canStudy = false, reason = "请先购买课程"
│
└─ 返回 Result<{ canStudy: boolean, reason?: string }>
```

### 3.9 章节结构（`GET /api/public/chapter-section/course/{courseId}/structure`）

```
│
┌─ 步骤1: 查询课程所有章节
│  └── chapterDao.queryByCourseId(courseId)
│      SQL: SELECT ... FROM chapter WHERE course_id = #{courseId}
│           ORDER BY sort ASC
│
┌─ 步骤2: 查询课程所有小节
│  └── sectionDao.queryByCourseId(courseId)
│      SQL: SELECT ... FROM section WHERE course_id = #{courseId}
│           ORDER BY sort ASC
│
┌─ 步骤3: 组装树形结构
│  ├── 遍历章节 → 从小节列表中过滤 chapterId 匹配的
│  └── 每节包含: id, title, sort, isFree, duration, videoUrl
│
└─ 返回 Result<List<ChapterStructureVO>>
    字段: id, title, sort, sections[{ id, title, sort, isFree, duration, videoUrl }]
    ⚠ videoUrl 对公共请求直接返回（前端可拼接视频地址）
```

### 3.10 加入课程（`POST /api/user/my-course/join`）

```
请求体: { courseId }
│
┌─ Sa-Token 校验: @SaCheckRole("用户") → userId
│
┌─ 步骤1: 查询课程
│  └── courseDao.queryById(courseId) + status 检查
│
┌─ 步骤2: 检查是否已加入
│  ├── myCourseDao.selectByUserAndCourse(userId, courseId)
│  └── 已存在 → "您已加入该课程"
│
┌─ 步骤3: 检查学习权限
│  ├── 免费课程 → 可直接加入
│  └── 付费课程 → 检查是否有已完成订单
│      ├── orderService.getCompletedOrderByUserAndCourse(userId, courseId)
│      └── 无订单 → "请先购买课程"
│
┌─ 步骤4: 创建 MyCourse 记录
│  ├── MyCourse {
│  │    userId, courseId,
│  │    status = 0 (已加入),
│  │    currentSectionId = 第一章第一节ID (如有),
│  │    progress = 0,
│  │    joinTime = new Date(),
│  │    lastStudyTime = new Date()
│  │  }
│  └── myCourseDao.insert(myCourse)
│
└─ 返回 Result<MyCourseVO>
    字段: id, courseId, userId, status, currentSectionId, progress, joinTime
```

### 3.11 我的课程列表（`GET /api/user/my-course/list`）

```
│
┌─ Sa-Token 校验: @SaCheckRole("用户") → userId
│
┌─ 步骤1: 查询条件
│  └── MyCourse { userId, status(可选) }
│
┌─ 步骤2: DAO 查询
│  └── myCourseDao.queryAllByLimit(condition)
│      SQL: SELECT ... FROM my_course WHERE user_id = #{userId}
│           [AND status = #{status}]
│           ORDER BY last_study_time DESC
│
┌─ 步骤3: 填充课程信息
│  └── 遍历 → courseDao.queryById(courseId) → 附加 Course 字段
│
└─ 返回 Result<List<MyCourseVO>>
    字段: id, courseId, userId, status, currentSectionId, progress,
          joinTime, lastStudyTime, course{...}
```

### 3.12 学习进度更新（`PUT /api/user/my-course/progress`）

```
请求体: { myCourseId, currentSectionId?, progress? }
│
┌─ Sa-Token 校验: @SaCheckRole("用户") → userId
│
┌─ 步骤1: 查询 MyCourse 记录
│  └── myCourseDao.queryById(myCourseId)
│
┌─ 步骤2: 归属校验
│  └── myCourse.userId != userId → "无权限"
│
┌─ 步骤3: 更新字段
│  ├── currentSectionId 非空 → 设置
│  ├── progress 非空 → 设置
│  ├── lastStudyTime = new Date()
│  └── status 检查:
│      ├── status=0 (已加入) → 自动升级为 status=1 (学习中)
│      └── status=1 → 检查是否完成（progress >= 100）
│          ├── 完成 → status=2, updateTime = now
│          └── 未完成 → 不变
│
┌─ 步骤4: 保存
│  └── myCourseDao.update(myCourse)
│
└─ 返回 Result.success("进度更新成功")
```

**MyCourse 状态流转**：

> 完整的学习状态定义与流转图见 [公共参考-学习状态体系](../../公共参考/公共数据模型与基础配置.md#五学习状态体系my_course)

本接口涉及：0(已加入) → 首次更新进度 → 1(学习中) → progress≥100 → 2(已完成)

### 3.13 退出课程（`POST /api/user/my-course/{id}/quit`）

```
│
┌─ Sa-Token 校验: @SaCheckRole("用户") → userId
│
┌─ 步骤1: 查询 MyCourse 记录
│
┌─ 步骤2: 归属校验
│
┌─ 步骤3: 删除记录（硬删除）
│  └── myCourseDao.deleteById(id)
│
└─ 返回 Result.success("已退出课程")
```

### 3.14 课程评论 — 列表（`GET /api/public/comment/course/{courseId}`）

```
请求参数: courseId + page, size
│
┌─ PageHelper 分页
│
┌─ DAO 查询
│  └── courseCommentDao.queryByCourseId(courseId)
│      SQL: SELECT ... FROM course_comment
│           WHERE course_id = #{courseId}
│           ORDER BY create_time DESC
│
└─ 返回 Result<PageInfo<CommentVO>>
    字段: id, courseId, userId, username, avatar, content, rating, createTime
```

### 3.15 课程评论 — 发表（`POST /api/user/comment`）

```
请求体: { courseId, content, rating }
│
┌─ Sa-Token 校验: @SaCheckLogin → userId
│
┌─ 步骤1: 检查是否已评论
│  └── courseCommentDao.selectByUserAndCourse(userId, courseId)
│      └── 已存在 → "您已评论过该课程"
│
┌─ 步骤2: 检查是否已加入课程
│  └── myCourseDao.selectByUserAndCourse(userId, courseId)
│      └── 未加入 → "请先加入课程后评论"
│
┌─ 步骤3: 创建评论
│  ├── CourseComment { userId, courseId, content, rating, createTime }
│  └── courseCommentDao.insert(comment)
│
└─ 返回 Result<CommentVO>
```

### 3.16 数据库表结构

> 数据库表结构详见 [数据库表结构汇总](../../公共参考/数据库表结构汇总.md)（course / chapter / section / my_course / course_comment）

### 3.17 关键 MyBatis 查询汇总

| SQL 方法 | 核心条件 | 排序 | 分页 |
|---|---|---|---|
| queryAllByLimit | status=1 + title LIKE + subjectId= + price=0 | create_time DESC | PageHelper |
| queryById | id= | — | — |
| queryByCourseId (chapter) | course_id= | sort ASC | — |
| queryByCourseId (section) | course_id= | sort ASC | — |
| selectByUserAndCourse (my_course) | user_id= AND course_id= | — | — |
| selectByUserAndCourse (comment) | user_id= AND course_id= | — | — |
| queryByCourseId (comment) | course_id= | create_time DESC | PageHelper |

---

## 四、前后端交互时序

### 课程列表加载

```
CourseList.vue onMounted
│
├── URL query 解析 (subjectId/title)
│
├── Promise.all ──────────────────────────────────────────────┐
│                                                             │
│  getCourses()  ← 4 分支 API                                 │
│  ├── 登录 + freeOnly → userApi.getFreeCourses(params)       │
│  ├── 未登录 + freeOnly → publicApi.getFreeCourses(params)   │
│  ├── 登录 → userApi.getCourseList(params)                   │
│  └── 未登录 → publicApi.getCourseList(params)               │
│                                                             │
│  publicApi.getCourseSubjectTree() → 分类侧栏               │
│  publicApi.getHotCourses(5) → 热门侧栏                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
│
├── 渲染课程网格 + 分类树 + 热门课程
└── 用户交互 → 搜索/筛选/翻页 → 重新调用列表 API
```

### 课程详情加载

```
CourseDetail.vue onMounted → route.params.id
│
├── getCourseDetail()
│   ├── 登录 → userApi.getCourseDetail(id) → 含个性化字段
│   └── 未登录 → publicApi.getCourseDetail(id) → 基础字段
│
├── getChapters()
│   └── publicApi.getCourseStructure(courseId)
│
├── 登录 → recordView(id)
│   └── POST /api/user/course/{id}/view → viewCount + 1
│
└── 用户操作:
    ├── 点击"购买" → 支付流程
    ├── 点击"加入" → POST /api/user/my-course/join
    ├── 点击"开始学习" → router.push('/courses/{id}/learn')
    └── 发表评论 → POST /api/user/comment
```

### 课程学习流程

```
CourseLearn.vue onMounted → courseId
│
├── 三级访问验证:
│   ├── 未登录 → AccessModal(login)
│   ├── userApi.canStudyCourse(courseId)
│   │   ├── canStudy=false → AccessModal(purchase/join)
│   │   └── 免费课程未加入 → 自动 joinCourse()
│   │
│   └── 验证通过 ↓
│
├── 加载数据:
│   ├── userApi.getCourseDetail(courseId) → 含 myCourseId
│   └── publicApi.getCourseStructure(courseId) → 章节树
│
├── 播放视频:
│   ├── 定位 currentSectionId → 设置 video src
│   └── timeupdate → 更新学习进度
│
└── 切换小节:
    ├── 检查访问权限（免费/已购/已加入）
    ├── 更新 video src + 播放
    └── myCourseApi.updateProgress(...)
```

---

## 五、已知问题与风险点

> 跨模块系统级问题见 [公共参考-系统级已知问题与风险清单](../../公共参考/系统级已知问题与风险清单.md)，此处仅列本模块独有问题。

| # | 问题 | 严重度 | 说明 |
|---|---|---|---|
| 1 | **浏览量非原子递增** | 高 | `viewCount = queryById().getViewCount() + 1; update()` 为读-改-写非原子操作，并发下丢失递增，应改为 `SET view_count = view_count + 1` |
| 2 | **免费课程可直接加入绕过购买** | 中 | 免费课程 `canStudy=true` 且 `joinCourse` 不检查订单，但付费课程加入需已完成订单；免费课程加入路径不经过支付，逻辑正确但需确认业务意图 |
| 3 | **章节 videoUrl 公开返回** | 中 | `GET /api/public/chapter-section/course/{courseId}/structure` 免登录返回完整 videoUrl，未购买用户可直接拼接地址访问付费视频 |
| 4 | **进度完成判定简化** | 低 | `progress >= 100` 判定完成，但进度值由前端上报，可被篡改 |
| 5 | **评论仅限已加入用户** | 低 | 评论需先加入课程，已购买但未加入的用户无法评论 |
| 6 | **免费筛选双重路径** | 低 | CourseList.vue 对 `freeOnly` 走独立 `/free` 端点而非在 `/page` 加 `price=0` 条件，API 路径冗余 |
| 7 | **视频 MIME 类型依赖浏览器** | 低 | HTML5 video 不指定 source type，依赖浏览器自动识别，部分格式可能不兼容 |
| 8 | **courseApi.ts fallback 吞噬错误** | 低 | userApi 失败后静默 fallback 到 publicApi，可能掩盖真实错误（如 403 权限问题被当作"未登录"处理） |
| 9 | **课程列表 userApi 无个性化差异** | 低 | 课程列表（非详情）userApi 与 publicApi 返回数据结构一致，登录与否对列表页无实质差异，4 分支选择增加复杂度但无收益 |
